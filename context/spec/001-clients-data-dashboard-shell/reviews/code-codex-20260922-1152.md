### F1: Empty child lists bypass the required shape rejection
- severity: major
- where: packages/contracts/src/index.ts:TreeNodeSchema.superRefine
- problem: Nodes carrying multiple kinds of child list are accepted when any additional list is empty, contrary to FR2 and FR4-AC10.
- evidence: Validation counts only non-empty lists; `index.test.ts` explicitly accepts `employees: [...]` alongside `channels: []`. The functional spec says “never two of these at once.”
- fix: Count defined child-list keys regardless of length; update the contradictory test and add a frontend rejection case.

### F2: Initial loading announcement is inserted already populated
- severity: major
- where: apps/web/src/pages/dashboard/ui/dashboard-page.tsx:loading status region
- problem: The initial render mounts the status region with “Loading clients…” already present, so the initial announcement required by FR3 is not reliably triggered.
- evidence: The region never exists empty before its initial loading text appears. Existing tests check DOM text and persistence after loading, which does not establish an initial announcement.
- fix: Mount the status region empty, populate it after mounting, and verify the initial announcement with a screen reader.

## Verdict

SHIP WITH FIXES

The shape validator and initial loading announcement need correction to meet the functional acceptance criteria.
---
_reviewer: codex · effort: low · kind: code · base: main · 2026-09-22T11:52:53+02:00_
