You are running as the **`react-frontend`** agent (`claude --agent react-frontend`): your instructions are `.claude/agents/react-frontend.md`; the skills it lists are in `.claude/skills/`. Read `CLAUDE.md` first.
This is a **consultation**, not a lane: answer the questions below in markdown. Read, search and run read-only commands as you need (verify versions and option names — do not guess). Create or edit **no files**, with one exception: when your answer is complete, write it in full to `context/spec/002-monthly-detail-table/consults/react-frontend-table-sections-20260922-145932.md` with the Write tool (that path is pre-approved), then end your turn with exactly this line:

    CONSULT react-frontend table-sections: DONE

The lead quotes the file verbatim — no preamble, no restating the questions; cite the files and commands you used to verify facts.

---

# Consultation for the technical specification of spec 002 — Monthly Detail Table

You are drafting the implementation plan for `context/spec/002-monthly-detail-table/technical-considerations.md`. The lead assembles the document; you answer the questions below. **Structures and contracts, not implementations**: file paths + responsibilities, component APIs (prop shapes), config keys, commands. No full code — short illustrative signatures are welcome, whole components are not. Verify facts by running things (this is a real worktree with the app installed) and say how you verified.

Read first:
- `context/spec/002-monthly-detail-table/functional-spec.md` — FR1–FR7, 44 acceptance criteria (the keyboard model in FR3 and the screen-reader expectations in FR4 are the heart of this feature)
- `context/inbox/monthly-detail-table.md` — the grill decisions D1–D16 and open risks R1–R5
- `context/product/architecture.md` — §1 (component library: none for the core, build the tree grid ourselves; the APG treegrid rule; the announcement rule), §4, §6 (the FSD layout: `shared/ui/tree-grid` is headless and knows nothing about clients; `entities/clients` knows the data and no DOM; `widgets` are the only place they meet)
- `context/inbox/design/tokens.md` — row height 56, name column 264, indent 28/level, chevron 16 + gap 8, avatar 20, header row, borders
- `context/inbox/design/{table-levels-1-2-opened.png,table-level-3-opened.png,row-name-variants.png}` — the design frames
- The shipped code from spec 001: `apps/web/src/{shared,entities,pages}`, especially `shared/ui/card`, `shared/styles/tokens.css`, `entities/clients/*`, `pages/dashboard/ui/dashboard-page.tsx` (the table card currently holds a placeholder summary you will replace)

The API, `packages/contracts` and `apps/api` are **not** touched by this feature. `childrenOf(node)` already exists in contracts and is the single definition of "children".

## Questions

1. **The DOM shape (risk R1 — prove this first).** A real `<table role="treegrid">` inside the existing `Card` (which has `overflow: clip` and an 8 px radius), with the first column sticky and the twelve month columns scrolling horizontally *inside the card* at narrow widths, while the page never scrolls sideways. Work out and **verify in a real browser** (start the dev server on a port that is free, e.g. 5273, and drive it with Playwright or the browser MCP): where the scroll container goes, what `position: sticky` needs on `th` in both the header row and the body rows, z-index against row borders and the header, whether the card's clipping must become the scroll container or wrap it, and how the radius survives. Report the structure that works, with the measurements you saw at 1440 and 375.
2. **Component boundaries and APIs.** What lives in `shared/ui/tree-grid` (headless, no business knowledge) versus `widgets/clients-table`? Give the public API you recommend — compound components (`TreeGrid`, `TreeGrid.Row`, `TreeGrid.Cell`, …) versus a hook returning props (`useTreeGrid` → `getRowProps`/`getCellProps`) versus both — and justify it against the brief's grading criterion: "Design component APIs the way you would on a real team: composable, with clear boundaries." Give the prop/`type` shapes (types, not interfaces — house rule). Say what the widget passes in and what it renders.
3. **The row model.** Where the tree becomes a flat list of visible rows (`entities/clients/model`), the shape of a row (id, parentId, level, position among siblings, sibling count, has-children, name, values), how expansion state is held (ids in a `Set`, in the widget or the hook?), and how the four ARIA attributes per FR4 are derived. Keep the pure transform unit-testable without a DOM.
4. **The keyboard model (FR3).** The full APG treegrid with row *and* cell focus. Give the complete key table you will implement, including: Right on a leaf enters its figures; Left from the first figure returns to the row's name; Home/End within a row when on a figure and first/last row when on a row; Enter/Space toggle on a row and do nothing on a figure; no wrapping at the edges. Then: how roving `tabindex` is managed across ~44 rows × 13 cells without a `tabindex` churn on every keystroke, and how focus is restored when a collapse removes the focused element (FR2 — the outline moves to the row just closed).
5. **Scrolling a focused cell into view (risk R2).** Moving onto an off-screen month must scroll the table horizontally only — never the page vertically. Recommend the approach you verified (`scrollIntoView({ block: 'nearest', inline: 'nearest' })` vs manual `scrollLeft` maths) and say what you observed with the sticky column in play.
6. **Animation (risk R3).** `@formkit/auto-animate` on the `<tbody>`: verify it actually animates `<tr>` insertion **and** removal (table rows notoriously ignore height transitions), that it respects `prefers-reduced-motion`, and that it does not break the sticky column or focus. Report what you measured. If it fails, recommend the fallback (entry-only CSS animation) and say so plainly.
7. **The name cell.** Indent per level (28 px steps inside the 264 px column), the chevron (the asset is `context/inbox/design/icon-chevron-down.svg`, rotated −90° when closed), the initials avatar for advisers (colour derived deterministically from the node id — give the scheme and how you keep contrast adequate), one-line ellipsis with the full name on hover **and on keyboard focus**, and the fact that only this cell toggles the row (D3) while figure cells stay inert.
8. **Tokens.** What `shared/styles/tokens.css` needs to gain: row height, name-column width, indent step, hover and focus treatments, the sticky-edge shadow, avatar colours. **The Figma MCP is rate-limited on the owner's seat, so the row hover and avatar colours could not be fetched** — propose values derived from the existing tokens (e.g. hover as a low-alpha tint of `--color-text`), name them so they are one-line swaps when the real values arrive, and say exactly which Figma nodes should be exported to confirm them (`0:1533` Row hover states, `0:1414` Row name variants).
9. **Tests.** Map the 44 acceptance criteria to layers: what is provable in Vitest + RTL (the whole keyboard model, ARIA attributes, the flatten transform, initials) and what genuinely needs a real browser in Playwright (sticky column, horizontal scroll and scroll-into-view, the animation, 375 px, axe in expanded states). Name the files and what each proves. Flag anything in FR4 that only a screen reader can confirm, since that becomes a `[User]` device check.
10. **Performance sanity.** 44 nodes today, but the widget should not re-render every row on each keystroke. Say what you would memoise and what you would deliberately not.

Aim for ~200–260 lines of markdown. End with **Risks I see** (max 5), each with the cheapest way to find out early.
