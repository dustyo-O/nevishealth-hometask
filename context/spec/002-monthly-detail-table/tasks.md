# Tasks — 002 Monthly Detail Table

Spec: `functional-spec.md` (FR1–FR7, 46 acceptance criteria) · How: `technical-considerations.md` (D-1…D-17, §2.1–2.5, §4) · Reviews: `reviews/TRIAGE.md` · The consult that measured it all: `consults/react-frontend-table-sections-20260922-145932.md`.

Everything lives in `apps/web`, so the slices are sequential and each is one lane — there is no second directory to parallelise into. Slice 0 is the lead's, and must land before the first lane starts. Slice 1 carries the riskiest unknown (the DOM shape that makes the sticky column work); nothing else can be built until it is settled.

## Standing rules

- Read `CLAUDE.md`, then `functional-spec.md`, then `technical-considerations.md`. The tech doc is the contract: its decisions were **measured in a real browser**, not guessed. Do not "improve" a value without re-measuring and saying so in your ledger note.
- `type` aliases over `interface`, everywhere. CSS Modules with tokens — no Tailwind, no inline style objects for static styles. FSD import direction downward only; every slice exposes `index.ts`.
- `shared/ui/tree-grid` must never import a client type. If a task seems to need one, you have put the code in the wrong layer — report it rather than reaching across.
- Tests are written with the change and proven RED first (temporarily revert, watch it fail, restore). Never delete or skip a test to make a gate green. A completion claim cites fresh output from this run.
- After a collapse, tests must `waitFor` the departing rows to leave the DOM rather than counting rows immediately (D-8: they linger ~250 ms, `aria-hidden` and `inert`).
- Gate before you finish: `pnpm check:web`. Not green → `PARTIAL`, with the failure quoted. The e2e server is on **5273** (`reuseExistingServer: false`); if you start a dev server for your own exploration, use another port or the gate will test the wrong tree.
- Every finished sub-task ends with `_(Done <date>: what actually happened, incl. surprises)_`.

---

- [ ] **Slice 0: The lint rule allows a tree grid (lead, before any lane)**

  > `<table role="treegrid">` trips `jsx-a11y/no-noninteractive-element-to-interactive-role`, whose allowlist is `table: ['grid']` (verified against this repo's own config). WAI-ARIA permits `treegrid`; the plugin's list is incomplete. The root config is not the frontend lane's to edit, so it lands first or the lane's first commit is red.
  - [ ] In the root `eslint.config.js`, add `treegrid` to the `jsx-a11y` rule's allowlist for `table` (D-14), with a one-line comment saying why. Run `pnpm --filter @nevis/web lint` to prove the config still loads clean. **[Lead]**

- [ ] **Slice 1: The table renders — real figures, right structure, sticky at 375**

  > The DOM shape first (grill R1, tech D-1…D-3): scroller `<div>` inside the Card, `<table role="treegrid">`, sticky name column. Rows come from a fixed expanded set (Company open) — no toggling yet, so this slice is about structure, semantics and geometry.
  - [ ] `entities/clients/model/flatten-rows.ts` + `initials.ts` per §2.3: `ClientRow`, `flattenVisibleRows(company, expandedIds)` (depth-first over `childrenOf` from `@nevis/contracts`, 1-based `level`/`posInSet`/`setSize`, `hasChildren` false for a missing **or** empty child list, `kind` from the level), `toInitials(name)`. Pure, no React. Tests RED first: levels, position/set size, leaf detection, a company with no branches, "Anna Blackwood" → "AB", one-word and accented names. **[Agent: react-frontend]**
  - [ ] `shared/ui/tree-grid` structure per §2.1 — `model/types.ts` and the components `TreeGrid` / `.Head` / `.ColumnHeader` / `.Row` / `.RowHeader` / `.Cell` / `.Toggle`, rendering from `TreeGridRow` with `aria-level`, `aria-posinset`, `aria-setsize`, and `aria-expanded` **only when the row has children**; `headers` on every figure and ids on every header (D-11). No keyboard yet — that is slice 3. CSS per D-2/D-3: `border-collapse: separate`, `border-spacing: 0`, `table-layout: fixed`, borders on cells not rows, sticky name `th` in both `thead` and `tbody` with an opaque background and `z-index: 2`, `scroll-padding-inline-start` on the scroller. **[Agent: react-frontend]**
  - [ ] Tokens per §2.5 into `shared/styles/tokens.css`: the table geometry block (row/header height, name column, indent step, paddings, chevron, avatar, `--table-month-col-min-w`), `--color-row-hover` and `--color-row-hover-solid` (measured from Figma `0:1533` — see `context/inbox/design/tokens.md` § Row states), `--shadow-sticky-edge`, the avatar tint variables. Mark the three proposals `/* proposal */` as `--color-skeleton` does. No token for an opened row: it has no background change. **[Agent: react-frontend]**
  - [ ] `widgets/clients-table` per §2.4, rendering a **fixed** expanded set (`new Set([company.id])`): the header row with a visually-hidden "Name" plus twelve month headings from `formatMonth`, one row per visible node, the name cell with indent (`--tree-grid-level`) and a reserved chevron slot, twelve right-aligned figures with tabular numerals shown exactly as stored. Swap the placeholder summary in `pages/dashboard` for the widget; leave the skeleton and error states untouched. **[Agent: react-frontend]**
  - [ ] Tests (RED first): `tree-grid.test.tsx` — the three level attributes, `aria-expanded` absent on leaves, `headers` on every figure, `jest-axe`; `clients-table.test.tsx` — twelve headings "Feb 2024"…"Jan 2025", figures verbatim **using a fixture whose parent value differs from the sum of its children** (review 2 F4 — the shipped data cannot prove this), the blank first header announced as "Name"; `dashboard-page.test.tsx` extended — the table replaces the summary, skeleton and error+Retry unchanged. **[Agent: react-frontend]**
  - [ ] Verify with the Playwright MCP against your own dev server (not 5273): at 1440 the card is 1408 wide, the name column 264, rows 56, `scrollWidth === clientWidth` (nothing scrolls); at 375 the scroller's `scrollWidth` exceeds its `clientWidth` while the page's does not, the name column stays pinned at the card's left edge while the months scroll, and the edge shadow appears only once scrolled. Check the Company row's figures read 250…350. Delete any screenshots afterwards. **[Agent: react-frontend]**
  - [ ] Merge `lane/002-s1-react-frontend`, run `pnpm check`, record ledger notes, `swarm.sh clean 002`. **[Lead]**

- [ ] **Slice 2: Open and close rows with the mouse**

  > The drill-down becomes real: the name cell toggles, rows are independent, and the name cell gets its finished look (chevron, avatar, ellipsis).
  - [ ] `useExpandedIds` in `shared/ui/tree-grid/model` (generic `Set<string>` + toggle) and the widget holds the value, seeded with the company id (D-6, controlled). **Collapsing prunes every descendant id** so re-opening a branch shows its children closed (FR2-AC3). **[Agent: react-frontend]**
  - [ ] The name cell's finished form: the chevron inlined as a component with `stroke="currentColor"` and `aria-hidden`, rotated −90° when closed (asset `context/inbox/design/icon-chevron-down.svg`); `shared/ui/avatar` drawing `toInitials` on the deterministic tint from D-12, `aria-hidden`, on adviser rows only; one-line ellipsis with `title` **and** the CSS reveal overlay shown on `:hover` and `:focus-visible` (D-13); hover applied to the cells with the opaque twin on the sticky one (D-5). Only the name `th` carries `onClick`; figure cells carry nothing. **[Agent: react-frontend]**
  - [ ] Tests (RED first): clicking the name opens and closes (FR2-AC1/AC2), pruning on re-open (FR2-AC3), independence (FR2-AC4), clicking a figure does nothing (FR2-AC5), initials on adviser rows and not elsewhere (FR5-AC1/AC2), `jest-axe` on an expanded table. **[Agent: react-frontend]**
  - [ ] Verify with the Playwright MCP: open Branch 1 → five advisers appear indented one step with avatars, the chevron rotates; open Anna Blackwood → three channels indented one further, their names aligning with the advisers' (the channel's extra indent replaces the avatar slot); close Branch 1 → everything beneath disappears; re-open → Anna is closed. Hover a row and confirm the tint holds across the sticky boundary at 375 while scrolled. Delete screenshots afterwards. **[Agent: react-frontend]**
  - [ ] Merge `lane/002-s2-react-frontend`, run `pnpm check`, record ledger notes, `swarm.sh clean 002`. **[Lead]**

- [ ] **Slice 3: The keyboard model**

  > The part the brief grades hardest. The reducer is pure and exhaustively testable; the hook wires it to focus, roving `tabindex` and scrolling.
  - [ ] `model/keyboard.ts` — the pure reducer `(cursor, key, rows, expandedIds) → { cursor } | { toggle } | null` implementing §2.2 exactly: ↑↓ in both modes, → expands or enters the figures (**including on a leaf**), ← collapses or goes to the parent, Home/End per mode, Enter/Space toggle on a row and do nothing on a figure, nothing wraps, and **Left on the closed Company row stays put** (review 2 F1). Tests RED first, every key in both modes, every edge. **[Agent: react-frontend]**
  - [ ] `model/use-tree-grid.ts` — cursor state, the props getters, roving `tabindex` by prop (`activeColIndex: number | null` to `memo`'d rows — two rows re-render per keystroke, D-9), the focus `useLayoutEffect` keyed on the cursor and guarded by `hasMovedRef` so mounting never steals focus, `focus({ preventScroll: true })` + `scrollIntoView({ block: 'nearest', inline: 'nearest' })` (D-7), and cursor recovery when a collapse removes the focused row (D-10). Wire it into the components and the widget. **[Agent: react-frontend]**
  - [ ] Tests (RED first): Tab enters the grid once and Tab leaves it (FR3-AC1); exactly one `tabindex="0"` at every step; Enter and Space toggle (FR3-AC12); focus recovery on collapse by mouse (FR2-AC6/AC7); the full walk from the acceptance criteria (FR3-AC2…AC13, AC15). **[Agent: react-frontend]**
  - [ ] Verify with the Playwright MCP: run the 19-step walk from the consult — Tab in, ↓ →, → onto the first figure, → → ←, Home/End, ← ← back out to Company, End then ↓ staying put — asserting `document.activeElement` and the single `tabindex="0"` at each step. Then at 375 with the page scrolled to `scrollY 300`: walk right and back and confirm `window.scrollY` never changes and no cell ever lands left of the sticky edge (280). Delete screenshots afterwards. **[Agent: react-frontend]**
  - [ ] Merge `lane/002-s3-react-frontend`, run `pnpm check`, record ledger notes, `swarm.sh clean 002`. **[Lead]**
  - [ ] Verify — device: with the app running, turn VoiceOver on (⌘F5) and move through the table with the arrow keys. Confirm a row announces its name, its level, "1 of 3", and open/closed; that a figure announces the row's name and the month with the number ("Anna Blackwood, Jun 2024, 32"); that opening a row says "expanded" and nothing more; and that the first column is announced as "Name". Report what you actually heard — this is the only proof of FR4, and spec 001 taught that hearing it late costs a rewrite. _(If you are not available, the lead may start slice 4 and leave this open — say so in the report rather than assuming a pass.)_ **[User]**

- [ ] **Slice 4: Rows slide in and out**

  > Motion last, because it is the only part that can hide a bug in the three slices beneath it.
  - [ ] Add `@formkit/auto-animate` to `apps/web` and hook `useAutoAnimate` onto `<tbody>` with a plugin returning **`translateY` keyframes** so rows literally slide (D-16, owner's decision), and the same plugin sets `aria-hidden="true"` and `inert` on every leaving node (D-15a, review 2 F3). Confirm the library still skips animation entirely under `prefers-reduced-motion` and note in the README that it reads that setting once at start-up. **[Agent: react-frontend]**
  - [ ] Tests (RED first): rows animate in and out (assert the WAAPI animations exist, then `waitFor` removal); under `prefers-reduced-motion: reduce` there are none and the rows leave within 50 ms (FR2-AC8); a leaving row carries `aria-hidden` and `inert` while it lingers. **[Agent: react-frontend]**
  - [ ] Verify with the Playwright MCP: expand and collapse Branch 1 at 1440 and at 375 mid-scroll, confirming the revealed rows move vertically, the sticky column, avatar and edge shadow hold during the collapse, and the table settles to the right row count; then toggle Branch 1 ten times rapidly and confirm the final count is right (the cheap check for the reconciler fighting the library). Delete screenshots afterwards. **[Agent: react-frontend]**
  - [ ] Merge `lane/002-s4-react-frontend`, run `pnpm check`, record ledger notes, `swarm.sh clean 002`. **[Lead]**

- [ ] **Slice 5: Feature Testing & Regression**

  > Verifies the whole feature end-to-end against functional-spec.md, run after all implementation slices are complete.
  - [ ] Read functional-spec.md acceptance criteria in full. Generate acceptance-level tests that verify the entire feature as a whole — not individual slices. Cover applicable layers (unit for pure logic, integration for service interactions, e2e for user flows) based on the project's testing stack. Write tests with RED validation (must fail before implementation is confirmed done). Annotate each test with `@spec: 002-monthly-detail-table` and `@regression` if suitable for long-term regression. **[Agent: testing-expert]**
  - [ ] Run all generated tests. All must pass. Fix any failures before proceeding. **[Agent: testing-expert]**
  - [ ] Install WebKit (`pnpm exec playwright install webkit`) and run the sticky and keyboard-scroll specs there as well as in Chromium (D-15): everything so far is proven in Chrome only, and the owner's screen-reader check runs on Safari's engine. Report any difference rather than papering over it. **[Agent: testing-expert]**
  - [ ] Merge `lane/002-s5-testing-expert`, run `pnpm check`, push, confirm CI green on the PR, record ledger notes, `swarm.sh clean 002`. **[Lead]**
  - [ ] Verify — device: repeat the VoiceOver pass from slice 3 on the finished table, at desktop width and at 375 px, including a collapse while focus is deep inside a branch. Confirm nothing is announced from rows that have just disappeared. **[User]**
