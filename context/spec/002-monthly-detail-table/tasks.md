# Tasks — 002 Monthly Detail Table

Spec: `functional-spec.md` (FR1–FR7, 46 acceptance criteria) · How: `technical-considerations.md` (D-1…D-17, §2.1–2.5, §4) · Reviews: `reviews/TRIAGE.md` · The consult that measured it all: `consults/react-frontend-table-sections-20260922-145932.md`.

Everything lives in `apps/web`, so the slices are sequential and each is one lane — there is no second directory to parallelise into. Slice 0 is the lead's, and must land before the first lane starts. Slice 1 carries the riskiest unknown (the DOM shape that makes the sticky column work); nothing else can be built until it is settled.

## Standing rules

- Read `CLAUDE.md`, then `functional-spec.md`, then `technical-considerations.md`. The tech doc is the contract: its decisions were **measured in a real browser**, not guessed. Do not "improve" a value without re-measuring and saying so in your ledger note.
- `type` aliases over `interface`, everywhere. CSS Modules with tokens — no Tailwind, no inline style objects for static styles. FSD import direction downward only; every slice exposes `index.ts`.
- `shared/ui/tree-grid` must never import a client type. If a task seems to need one, you have put the code in the wrong layer — report it rather than reaching across.
- Tests are written with the change and proven RED first (temporarily revert, watch it fail, restore). Never delete or skip a test to make a gate green. A completion claim cites fresh output from this run.
- After a collapse, tests must `waitFor` the departing rows to leave the DOM rather than counting rows immediately (D-8: they linger ~250 ms, `aria-hidden` and `inert`).
- Gate before you finish: `pnpm check:web`. Not green → `PARTIAL`, with the failure quoted. The e2e server is on **5273** (`reuseExistingServer: false`); if you start a dev server for your own exploration, use another port or the gate will test the wrong tree.
- Every finished sub-task ends with `_(Done <date>: what actually happened, incl. surprises)_`.

---

- [x] **Slice 0: The lint rule allows a tree grid (lead, before any lane)**

  > `<table role="treegrid">` trips `jsx-a11y/no-noninteractive-element-to-interactive-role`, whose allowlist is `table: ['grid']` (verified against this repo's own config). WAI-ARIA permits `treegrid`; the plugin's list is incomplete. The root config is not the frontend lane's to edit, so it lands first or the lane's first commit is red.
  - [x] In the root `eslint.config.js`, add `treegrid` to the `jsx-a11y` rule's allowlist for `table` (D-14), with a one-line comment saying why. Run `pnpm --filter @nevis/web lint` to prove the config still loads clean. **[Lead]** _(Done 2026-09-22: added a `rules` block to the apps/web config overriding jsx-a11y/no-noninteractive-element-to-interactive-role with `table: ['grid','treegrid']` (the plugin's other defaults restated so nothing is lost), plus a comment citing the APG treegrid pattern. Proven both ways: `pnpm --filter @nevis/web lint` exit 0, and a throwaway `<table role="treegrid">` probe linted clean before being deleted.)_

- [x] **Slice 1: The table renders — real figures, right structure, sticky at 375**

  > The DOM shape first (grill R1, tech D-1…D-3): scroller `<div>` inside the Card, `<table role="treegrid">`, sticky name column. Rows come from a fixed expanded set (Company open) — no toggling yet, so this slice is about structure, semantics and geometry.
  - [x] `entities/clients/model/flatten-rows.ts` + `initials.ts` per §2.3: `ClientRow`, `flattenVisibleRows(company, expandedIds)` (depth-first over `childrenOf` from `@nevis/contracts`, 1-based `level`/`posInSet`/`setSize`, `hasChildren` false for a missing **or** empty child list, `kind` from the level), `toInitials(name)`. Pure, no React. Tests RED first: levels, position/set size, leaf detection, a company with no branches, "Anna Blackwood" → "AB", one-word and accented names. **[Agent: react-frontend]** _(Done 2026-09-22: flattenVisibleRows walks childrenOf depth-first with 1-based level/posInSet/setSize, a leaf for a missing *or* empty list, kind named from the level; toInitials spreads rather than indexes so accented first letters survive. 15 tests, RED first. toInitials has no caller until slice 2 — as the task was written. Commit 11b58a8)_
  - [x] `shared/ui/tree-grid` structure per §2.1 — `model/types.ts` and the components `TreeGrid` / `.Head` / `.ColumnHeader` / `.Row` / `.RowHeader` / `.Cell` / `.Toggle`, rendering from `TreeGridRow` with `aria-level`, `aria-posinset`, `aria-setsize`, and `aria-expanded` **only when the row has children**; `headers` on every figure and ids on every header (D-11). No keyboard yet — that is slice 3. CSS per D-2/D-3: `border-collapse: separate`, `border-spacing: 0`, `table-layout: fixed`, borders on cells not rows, sticky name `th` in both `thead` and `tbody` with an opaque background and `z-index: 2`, `scroll-padding-inline-start` on the scroller. **[Agent: react-frontend]** _(Done 2026-09-22: compound surface, no client type anywhere; aria-expanded only where hasChildren; explicit headers on every figure from ids the grid generates itself so they cannot drift. Two deviations: §2.1 named no <tbody> owner, so TreeGrid takes the header row as a `head` slot and owns <tbody> itself (slice 4's useAutoAnimate ref belongs there); the compound statics are attached one by one because Object.assign makes react-refresh/only-export-components lose sight of the component (lint error, reproduced). Commit 4eacfc6)_
  - [x] Tokens per §2.5 into `shared/styles/tokens.css`: the table geometry block (row/header height, name column, indent step, paddings, chevron, avatar, `--table-month-col-min-w`), `--color-row-hover` and `--color-row-hover-solid` (measured from Figma `0:1533` — see `context/inbox/design/tokens.md` § Row states), `--shadow-sticky-edge`, the avatar tint variables. Mark the three proposals `/* proposal */` as `--color-skeleton` does. No token for an opened row: it has no background change. **[Agent: react-frontend]** _(Done 2026-09-22: geometry from 1:2901, the hover pair from 0:1533 with its opaque twin, proposals marked. No token for an opened row. The tech doc left the edge shadow's trigger open: no CSS alone toggles a box-shadow on scrollLeft portably, so the scroller writes data-scrolled straight to the DOM in its scroll handler (no state, no re-render) and the stylesheet decides what it means — verified appearing at scrollLeft 400 and gone at 0. Commit 38c087c)_
  - [x] `widgets/clients-table` per §2.4, rendering a **fixed** expanded set (`new Set([company.id])`): the header row with a visually-hidden "Name" plus twelve month headings from `formatMonth`, one row per visible node, the name cell with indent (`--tree-grid-level`) and a reserved chevron slot, twelve right-aligned figures with tabular numerals shown exactly as stored. Swap the placeholder summary in `pages/dashboard` for the widget; leave the skeleton and error states untouched. **[Agent: react-frontend]** _(Done 2026-09-22: twelve headings from formatMonth, visually-hidden "Name", indent via --tree-grid-level, reserved chevron slot, tabular figures verbatim; widget owns useClientsQuery (TanStack dedupes with the page's); the table is labelled "Clients by month" rather than repeating the card's "Monthly detail"; skeleton and error states untouched. Commit da60b08)_
  - [x] Tests (RED first): `tree-grid.test.tsx` — the three level attributes, `aria-expanded` absent on leaves, `headers` on every figure, `jest-axe`; `clients-table.test.tsx` — twelve headings "Feb 2024"…"Jan 2025", figures verbatim **using a fixture whose parent value differs from the sum of its children** (review 2 F4 — the shipped data cannot prove this), the blank first header announced as "Name"; `dashboard-page.test.tsx` extended — the table replaces the summary, skeleton and error+Retry unchanged. **[Agent: react-frontend]** _(Done 2026-09-22: 77 pass (13 files), every new file RED first; figures proven against a fixture whose Company is nothing like the sum of its branches (250 vs 3). Surprise: placeholderBlocksOf in dashboard-page.test.tsx matched [aria-hidden="true"], which now also matches every row's chevron slot, so a loaded table read as "still loading" — it now matches the Skeleton class. The same trap sits at e2e/loading.spec.ts:22)_
  - [x] Verify with the Playwright MCP against your own dev server (not 5273): at 1440 the card is 1408 wide, the name column 264, rows 56, `scrollWidth === clientWidth` (nothing scrolls); at 375 the scroller's `scrollWidth` exceeds its `clientWidth` while the page's does not, the name column stays pinned at the card's left edge while the months scroll, and the edge shadow appears only once scrolled. Check the Company row's figures read 250…350. Delete any screenshots afterwards. **[Agent: react-frontend]** _(Done 2026-09-22: own server on 5373 + the real API. 1440: card 1408×280, name column 264, header and rows 56, scrollWidth === clientWidth, no shadow, Company reads 250…350. 375: scroller 343/1344 scrolls while the page stays 375/375, the name column holds at 0 px through scrollLeft 400 → end → 0, opaque with no bleed-through, shadow only while scrolled; indents match tokens.md (Company 40, Branch 68); last row carries no divider. Screenshots deleted, servers stopped. Re-measured D-17: 72 px wrapped every heading → 88 px, scroll width ≈1344 (commit b7398d1))_
  - [x] Spec 001's acceptance e2e asserts the placeholder summary this slice replaces (FR7 obsoletes it): update `apps/web/e2e/{a11y,error,loaded,loading,retry,switches,viewport-375,prod/switches}.spec.ts` and `e2e/support/clients-page.ts` so "settled" means the table is showing (the Company row and its branches) instead of `TEXT.branches`. Fix the same `[aria-hidden]` trap the lane hit, at `loading.spec.ts:22`: a loaded table now contains `aria-hidden` chevron slots, so match the Skeleton class rather than the attribute. Keep every other assertion in those files intact — they are spec 001's and still true. **[Agent: testing-expert]** _(Done 2026-09-22: a new expectTableLoaded(ui, names) waits on the treegrid and its row headers across eight specs, so each fixture asserts its own rows (3 branches / 2 / 1 / none) instead of a sentence; RED proven by reverting and watching all 5 summary assertions fail. FR5-AC2/AC3 and the D-12 "1 branch" test only ever asserted that line, so they were re-pointed at row lists — D-12's singular wording died with the line it described. viewport-375's "both summaries readable" loop keeps the chart card's paragraph check and asserts every row name's right edge stays inside 375 for the table. The [aria-hidden] trap at loading.spec.ts:22 now matches Vite's scoped _skeleton_ class (verified in the built CSS). Commit 429a238. Then, at the lead's request, the 375-px loaded axe case alone is test.fixme with D-9, the FR3-AC1 conflict and slice 3 named as its removal — blast radius exactly one case (commit b36ec8a))_
  - [x] Merge `lane/002-s1-react-frontend`, run `pnpm check`, record ledger notes, `swarm.sh clean 002`. **[Lead]** _(Done 2026-09-22: both lanes merged --no-ff (d49d861, dbc7e84), no conflicts; the lead also re-measured D-17 into the tech doc (72 → 88 px). `pnpm check` exit 0 on the merged tree: prettier ✓, contracts 18, api 69 + production smoke, web lint ✓ typecheck ✓ unit 77, e2e 39 passed / 1 skipped (the documented fixme). Panes and worktrees cleaned)_

- [x] **Slice 2: Open and close rows with the mouse**

  > The drill-down becomes real: the name cell toggles, rows are independent, and the name cell gets its finished look (chevron, avatar, ellipsis).
  - [x] `useExpandedIds` in `shared/ui/tree-grid/model` (generic `Set<string>` + toggle) and the widget holds the value, seeded with the company id (D-6, controlled). **Collapsing prunes every descendant id** so re-opening a branch shows its children closed (FR2-AC3). **[Agent: react-frontend]** _(Done 2026-09-22: generic Set<string> + toggle(id, rows) in shared/ui/tree-grid/model; toggle takes the rendered rows because pruning needs parent links and the hook must stay client-agnostic. The widget splits in two so the inner component mounts with the company id as its seed — seeding lazily from a query that starts undefined would have left the Company row closed when the data arrived. Commit 5c1fcf8)_
  - [x] The name cell's finished form: the chevron inlined as a component with `stroke="currentColor"` and `aria-hidden`, rotated −90° when closed (asset `context/inbox/design/icon-chevron-down.svg`); `shared/ui/avatar` drawing `toInitials` on the deterministic tint from D-12, `aria-hidden`, on adviser rows only; one-line ellipsis with `title` **and** the CSS reveal overlay shown on `:hover` and `:focus-visible` (D-13); hover applied to the cells with the opaque twin on the sticky one (D-5). Only the name `th` carries `onClick`; figure cells carry nothing. **[Agent: react-frontend]** _(Done 2026-09-22: chevron inlined with currentColor/aria-hidden, rotation driven by the row's own aria-expanded rather than a class so the arrow and the screen reader cannot disagree (no transition — motion is slice 4's contract). D-13's reveal is the label itself promoted to an absolutely positioned opaque box, not a second copy: a duplicate would have put "ABAnna Blackwood" into every textContent read; measured at 375 scrolled, its right edge lands at 275 against a sticky boundary of 280, so it never spills over the months. The grid publishes --tree-grid-name-bg so the widget's overlay matches the row's hover twin across the module boundary. onClick on the th raises no jsx-a11y error, as the tech doc claimed. Commit f334aa4)_
  - [x] Tests (RED first): clicking the name opens and closes (FR2-AC1/AC2), pruning on re-open (FR2-AC3), independence (FR2-AC4), clicking a figure does nothing (FR2-AC5), initials on adviser rows and not elsewhere (FR5-AC1/AC2), `jest-axe` on an expanded table. **[Agent: react-frontend]** _(Done 2026-09-22: 101 unit in 15 files, every new test watched fail first (the FR2 set before the widget was wired, FR5 before the avatar, the chevron pair before the icon); pruning re-proven by reverting its one line — FR2-AC3/AC4 went red, then restored. jest-axe clean with Branch 1 and Anna open. Surprise: visibleNames() by textContent broke once the avatar landed inside the row header and now reads the label — the same trap is live at e2e/support/clients-page.ts:74 for any future spec that opens a branch (testing-expert's file))_
  - [x] Verify with the Playwright MCP: open Branch 1 → five advisers appear indented one step with avatars, the chevron rotates; open Anna Blackwood → three channels indented one further, their names aligning with the advisers' (the channel's extra indent replaces the avatar slot); close Branch 1 → everything beneath disappears; re-open → Anna is closed. Hover a row and confirm the tint holds across the sticky boundary at 375 while scrolled. Delete screenshots afterwards. **[Agent: react-frontend]** _(Done 2026-09-22: Branch 1 → five advisers at level 3 with avatars AB/JW/MG/RC/SS, chevron unrotates; Anna → three channels at level 4 whose names sit at x124, flush with the advisers'; closing Branch 1 leaves Company + three branches; re-opening shows Anna aria-expanded="false". At 375 (scroller 1344/343, page 375/375) hovering gives the opaque twin on the sticky cell and the translucent wash on the figures, shadow showing. Screenshots and .playwright-mcp deleted, servers stopped. The worktree had no node_modules — pnpm install --frozen-lockfile first)_
  - [x] Merge `lane/002-s2-react-frontend`, run `pnpm check`, record ledger notes, `swarm.sh clean 002`. **[Lead]** _(Done 2026-09-22: merged --no-ff (443f9ea), no conflicts; `pnpm check` exit 0 on the merged tree: prettier ✓, contracts 18, api 69 + smoke, web lint ✓ typecheck ✓ unit 101 (15 files), e2e 39 passed / 1 skipped (the slice-1 fixme slice 3 removes); pane and worktree cleaned)_

- [x] **Slice 3: The keyboard model**

  > The part the brief grades hardest. The reducer is pure and exhaustively testable; the hook wires it to focus, roving `tabindex` and scrolling.
  - [x] `model/keyboard.ts` — the pure reducer `(cursor, key, rows, expandedIds) → { cursor } | { toggle } | null` implementing §2.2 exactly: ↑↓ in both modes, → expands or enters the figures (**including on a leaf**), ← collapses or goes to the parent, Home/End per mode, Enter/Space toggle on a row and do nothing on a figure, nothing wraps, and **Left on the closed Company row stays put** (review 2 F1). Tests RED first, every key in both modes, every edge. **[Agent: react-frontend]** _(Done 2026-09-22: the whole of §2.2 as one pure function, 21 tests, RED first against a stub (18 failed, 3 passed — the 3 were re-checked, since a test passing against a no-op may assert nothing). Two notes for the doc: `columnCount` is a fifth argument the tech doc's signature lacks ("stop at 11" is counted from it, and a TreeGridRow has no figures to count, as it must not); and an edge returns the cursor it was given rather than null, so the caller still swallows the key — an arrow falling through to the browser scrolls the months under a stationary outline, and Space scrolls the page, neither of which is "the outline stays where it is". Commit 45277f9. _(This lane's first session died mid-task with an API connection loss; it was resumed in its own worktree and judged its own leftovers.)_)_
  - [x] `model/use-tree-grid.ts` — cursor state, the props getters, roving `tabindex` by prop (`activeColIndex: number | null` to `memo`'d rows — two rows re-render per keystroke, D-9), the focus `useLayoutEffect` keyed on the cursor and guarded by `hasMovedRef` so mounting never steals focus, `focus({ preventScroll: true })` + `scrollIntoView({ block: 'nearest', inline: 'nearest' })` (D-7), and cursor recovery when a collapse removes the focused row (D-10). Wire it into the components and the widget. **[Agent: react-frontend]** _(Done 2026-09-22: cursor, roving tabindex by prop, focus layout effect guarded by hasMovedRef, preventScroll + scrollIntoView, D-10 recovery. The widget's row moved into a memo'd clients-row.tsx: a `children` prop would have defeated the memo and with it D-9's two-rows-per-keystroke. Three deviations from §2.1's sketch, each measured: props getters would hand every row a fresh object each render (components keep their own props; the hook supplies activeColIndexOf/onKeyDown/toggle); useTreeGrid takes the grid's id rather than a cellId function, since D-11's point is that ids are generated in one place; D-10's lastToggled is state not a ref, because react-hooks/refs rejects reading a ref during render. D-4 needed --focus-ring split into width/colour — the focus ring and the sticky edge shadow both land on the name cell through box-shadow, so whichever rule came last would have erased the other. Commits 5837faf, cc6bf14)_
  - [x] Tests (RED first): Tab enters the grid once and Tab leaves it (FR3-AC1); exactly one `tabindex="0"` at every step; Enter and Space toggle (FR3-AC12); focus recovery on collapse by mouse (FR2-AC6/AC7); the full walk from the acceptance criteria (FR3-AC2…AC13, AC15). **[Agent: react-frontend]** _(Done 2026-09-22: the grid test gains a live harness walking FR3-AC1…AC15 and the FR2-AC6/AC7 recovery, its `press` helper asserting after every keystroke that exactly one element has tabindex="0" and that it is the focused element; the widget test proves the wiring separately, because a harness that rebuilds it by hand stays green when the real widget drops onKeyDown (proven: that revert is 2/20 red). RED six ways: no focus effect 12/24, no D-10 recovery 1/24, no roving tabindex 15/24, widget without onKeyDown 2/20, activeColIndex pinned null 4/20, toggling around the grid instead of through it 1/20)_
  - [x] Confirm the axe violation slice 1 left open is closed: at 375 the scroller reported `scrollable-region-focusable` ("Scrollable region must have keyboard access") because nothing inside it was focusable yet. The lane proved it passes once the rows carry a roving tabindex — assert 0 violations at 375 in an axe run, and do **not** add `tabindex="0"` to the scroller (that would be a second tab stop and contradict FR3-AC1). **[Agent: react-frontend]** _(Done 2026-09-22: test.fixme deleted, not suppressed, and the scroller still carries no tabindex of its own; "at 375 px (phone) › the loaded state has no axe violations" passes and the suite went 39 passed/1 skipped → 40 passed. Commit 6879424)_
  - [x] Verify with the Playwright MCP: run the 19-step walk from the consult — Tab in, ↓ →, → onto the first figure, → → ←, Home/End, ← ← back out to Company, End then ↓ staying put — asserting `document.activeElement` and the single `tabindex="0"` at each step. Then at 375 with the page scrolled to `scrollY 300`: walk right and back and confirm `window.scrollY` never changes and no cell ever lands left of the sticky edge (280). Delete screenshots afterwards. **[Agent: react-frontend]** _(Done 2026-09-22: own server on 5373 + the real API, both stopped after, artefacts deleted. At 1440 a 25-key walk (the consult's 19 plus the End/↓ and Left-on-closed-Company edges): every transition as specified, Branch 1's Feb 2024 reads 147 exactly as the consult measured, and at every step exactly one tabindex="0" on the focused element; a further Tab leaves the grid; mounting steals no focus. At 375 with the page pre-scrolled to scrollY 300: scrollY never moved across 26 steps, but the leftward walk landed every cell at 271 against a sticky edge of 280 — a D-17-vs-D-7 regression the 72 → 88 px column change introduced. Fixed (commit 7ee3349): every cell now lands at exactly 280 in both directions, scrollY still 300, and the 1440 walk byte-for-byte unchanged)_
  - [x] FR3 amended after the owner's screen-reader pass: **the arrows must no longer open or close rows.** In `model/keyboard.ts`, Right always moves to `colIndex 0` (never expands, whatever the row's state) and Left always moves to the parent row (never collapses; on the Company row it stays put); Enter and Space remain the only keys that toggle, and do nothing on a row with no children. Update the reducer's tests to the new table — several currently assert the old expand/collapse behaviour, so they must change with it, RED first — and re-run the live harness walk. **[Agent: react-frontend]** _(Done 2026-09-22: Right always moves to colIndex 0, Left always to the parent (staying put on Company); Enter/Space remain the only toggles. Three surprises: the reducer's expandedIds became entirely unread and TS's noUnusedParameters forced a `_` prefix — the lane left §2.1's signature and UseTreeGridOptions.expandedIds intact rather than narrowing a public contract from inside a lane (prune later if wanted); the old behaviour was also asserted in five DOM-level tests, not just the reducer's (3 in tree-grid.test.tsx, 2 in clients-table.test.tsx), all rewritten, 4 proved RED against the reverted reducer and the 5th passes either way since only its way of expanding changed; and e2e/loading.spec.ts flaked twice under parallel load on a different assertion each time before passing 40/40 three times including on an unmodified HEAD — not from this change, ticket-worthy. Gate green: 142 unit + 40 e2e; live harness walk re-run against FR3-AC1…AC15. Commit 1f98c5e)_
  - [x] `e2e/loading.spec.ts` measures the announcement from page-open but the page's own one-second timer starts at **mount**, so under the full suite's parallel load (four browsers on one dev server) mount lands ~2 s in and `spokeAfter` reaches 3090 ms — tripping `expect(spokeAfter).toBeLessThan(DELAY_MS)`, which compares the page's clock against the *service's* 3000 ms delay, a different clock entirely. Both viewports fail in a full run and all four pass when the file runs alone; the merged tree is byte-identical to the lane's. Fix the coupling, not the timing: record when the figures land the same way the announcement is recorded (an init-script mark), and assert the announcement came **after `ANNOUNCE_DELAY_MS` and before the figures landed** — which is what FR3 actually promises and is invariant under load. Keep the rest of the file as it is. **[Agent: testing-expert]** _(Done 2026-09-22: reproduced under 8 CPU hogs + 8 workers — spokeAfter hit 3083 ms (375) and 3484 ms (1440) against the old 3000 bound, and even idle it ran 2178–2455 ms, so the assertion had ~550 ms of headroom rather than the ~2 s it looked like. recordAnnouncement is now recordTimings: the same init-script observer marks the treegrid's arrival (__figuresLandedMs) off the page's own performance.now(), and the test asserts the announcement fell after ANNOUNCE_DELAY_MS and before the figures — FR3-AC2's actual promise, invariant under load. Chose the treegrid over aria-busy="false" because the error view also flips that flag. RED proven by inverting to toBeGreaterThan: both viewports failed with real numbers (2178 vs 4202, 2455 vs 4511). Surprise, left as a ticket: the same coupling bites retry.spec.ts:39 and switches.spec.ts:45 under contention, both passing at the gate's 4 workers. Commit 54ea078)_
  - [x] Merge `lane/002-s3-react-frontend`, run `pnpm check`, record ledger notes, `swarm.sh clean 002`. **[Lead]** _(Done 2026-09-22: both lanes merged --no-ff (1df3f77, 9f27011); `pnpm check` exit 0 on the merged tree: prettier ✓, contracts 18, api 69 + smoke, web lint ✓ typecheck ✓ unit 142, e2e 40 passed (the slice-1 fixme gone, nothing skipped); panes and worktrees cleaned)_
  - [x] Verify — device: with the app running, turn VoiceOver on (⌘F5) and move through the table with the arrow keys. Confirm a row announces its name, its level, "1 of 3", and open/closed; that a figure announces the row's name and the month with the number ("Anna Blackwood, Jun 2024, 32"); that opening a row says "expanded" and nothing more; and that the first column is announced as "Name". Report what you actually heard — this is the only proof of FR4, and spec 001 taught that hearing it late costs a rewrite. _(If you are not available, the lead may start slice 4 and leave this open — say so in the report rather than assuming a pass.)_ **[User]** _(Done 2026-09-22: owner ran it — "voiceover works impressive!", so FR4's announcements are confirmed by the only proof that counts. One finding, now a spec change: "right arrow expands the row => this confuses during navigation. let's expand only with enter/space" — FR3 amended, arrows move only.)_

- [x] **Slice 4: Rows slide in and out**

  > Motion last, because it is the only part that can hide a bug in the three slices beneath it.
  - [x] Add `@formkit/auto-animate` to `apps/web` and hook `useAutoAnimate` onto `<tbody>` with a plugin returning **`translateY` keyframes** so rows literally slide (D-16, owner's decision), and the same plugin sets `aria-hidden="true"` and `inert` on every leaving node (D-15a, review 2 F3). Confirm the library still skips animation entirely under `prefers-reduced-motion` and note in the README that it reads that setting once at start-up. **[Agent: react-frontend]** _(Done 2026-09-22: 0.10.0 on <tbody> with a plugin that slides rows (arriving from one row-height above, leaving upwards into their parent) and takes a leaving row out of the a11y tree (aria-hidden + inert). **D-16's premise was false**: the library only skips animation under prefers-reduced-motion when it has no plugin — its source reads `mediaQuery.matches && !isPlugin(config)`, so D-8 measured the default options; rowMotion() therefore hands it plain options under reduce, switching it off outright (0 animations, rows leave in one frame / 7 ms). Two files outside apps/web, both named by the task: pnpm-lock.yaml and the README note that the setting is read once at start-up. Commit 04151d5)_
  - [x] Tests (RED first): rows animate in and out (assert the WAAPI animations exist, then `waitFor` removal); under `prefers-reduced-motion: reduce` there are none and the rows leave within 50 ms (FR2-AC8); a leaving row carries `aria-hidden` and `inert` while it lingers. **[Agent: react-frontend]** _(Done 2026-09-22: 155 unit in 18 files. RED five ways: tbody unhooked (3 fail), D-15a attributes dropped (2), reduced motion ignored (2), the library's own scale-and-fade instead of a slide (4), and the old useAutoAnimate wiring under StrictMode (1). jsdom lacks WAAPI, ResizeObserver (what the library probes to decide it is in a browser) and matchMedia, so the new files stub them as setup.ts already does for scrollIntoView. Note for future tests: the library measures rows on a 500 ms debounce, so a row closed sooner is one it never knew and never animates)_
  - [x] Verify with the Playwright MCP: expand and collapse Branch 1 at 1440 and at 375 mid-scroll, confirming the revealed rows move vertically, the sticky column, avatar and edge shadow hold during the collapse, and the table settles to the right row count; then toggle Branch 1 ten times rapidly and confirm the final count is right (the cheap check for the reconciler fighting the library). Delete screenshots afterwards. **[Agent: react-frontend]** _(Done 2026-09-22: found three defects the unit tests could not, all fixed and re-measured (commit 4ef482b): (1) arriving rows never moved — useAutoAnimate attaches from a ref callback it cannot undo, so StrictMode's double mount left two observers on one tbody and the second cancelled every entry slide ~1 ms in; now an effect with cleanup, guarded by a StrictMode test; (2) a leaving row kept its ids, so re-opening inside the ~250 ms window put duplicates in the DOM and getElementById answered with the inert ghost (reproduced: Down left the outline nowhere) — a leaving row now gives up its ids, 5 duplicates → 0; (3) cells glued together during a collapse, because position:absolute blockifies the <tr> and drops its cells out of the fixed column layout (a month cell measured 34 px against 88) — the stylesheet hands the leaving row the table's layout back, now 264/88×11/112, identical to a live row. Final: entering rows travel −56→0 px with opacity 0→1, departing 0→−55 with 1→0 while the rows below close 280 px; at 375/scrollLeft 400 the sticky column, avatar and shadow hold through the collapse; ten toggles at 60 ms leave four live rows, nothing lingering. Library rule worth knowing: it skips the entry animation of a row below the fold)_
  - [x] Merge `lane/002-s4-react-frontend`, run `pnpm check`, record ledger notes, `swarm.sh clean 002`. **[Lead]** _(Done 2026-09-22: merged --no-ff (7ff31de). First gate run failed lint with unresolved @formkit types — the lead's tree had not installed the lane's new dependency; `pnpm install --frozen-lockfile` then `pnpm check` exit 0: prettier ✓, contracts 18, api 69 + smoke, web lint ✓ typecheck ✓ unit 155 (18 files), e2e 40 passed; pane and worktree cleaned)_

- [ ] **Slice 5: Feature Testing & Regression**

  > Verifies the whole feature end-to-end against functional-spec.md, run after all implementation slices are complete.
  - [x] Read functional-spec.md acceptance criteria in full. Generate acceptance-level tests that verify the entire feature as a whole — not individual slices. Cover applicable layers (unit for pure logic, integration for service interactions, e2e for user flows) based on the project's testing stack. Write tests with RED validation (must fail before implementation is confirmed done). Annotate each test with `@spec: 002-monthly-detail-table` and `@regression` if suitable for long-term regression. **[Agent: testing-expert]** _(Done 2026-09-22: browser acceptance suite for FR1–FR7 on the shipped data, plus an opt-in WebKit project over every table spec (commits fe7be97, 89a2c27, 8b45b96). After the spec amendments it brought table-truncation.spec.ts in line with the new FR5 — 1440 still asserts nothing is shortened, and a new 375 test checks the deep names are ellipsised and each still has a full title, a full accessible name, and shows whole on hover and on keyboard focus (505ea0c). Its own words: the tests changed because the spec changed, not the implementation)_
  - [x] Run all generated tests. All must pass. Fix any failures before proceeding. **[Agent: testing-expert]** _(Done 2026-09-22: the suite found two production defects the unit tests could not and the lane refused to fix either, both being outside its files. (1) A revealed name painted no background — its box was 0 px tall, so the figure behind showed through as "Anna Blackwood25"; 5 tests red at both widths. It verified a fix (.nameSlot { align-self: stretch; min-block-size: 1lh }) made all 7 pass and reverted it without committing. (2) In WebKit only, and every time (3/3 with --repeat-each=3 --workers=1), auto-animate threw at deletePosition on collapse and left the departing row in the DOM, not inert — 4 specs red, and it flagged that this blocked the owner's Safari check)_
  - [x] Install WebKit (`pnpm exec playwright install webkit`) and run the sticky and keyboard-scroll specs there as well as in Chromium (D-15): everything so far is proven in Chrome only, and the owner's screen-reader check runs on Safari's engine. Report any difference rather than papering over it. **[Agent: testing-expert]** _(Done 2026-09-22: WebKit installed; the sticky and keyboard-scroll specs pass identically in both engines — 19 passed (setup + 9 Chromium + 9 WebKit), no difference, which answers D-15's question. Running every table spec in WebKit is what surfaced the collapse defect above (51 passed / 9 failed at the time; after the fix, 60 passed))_
  - [x] The acceptance suite's 375-px run found a real defect and the owner has ruled on it (FR6 amended, tech doc D-17a): **the name column narrows to 160 px below ~600 px viewport width**, so two whole month columns fit beside it instead of a sliver of one. Add the responsive rule in `shared/ui/tree-grid`'s stylesheet as a token override (`--table-name-col-w`), keeping 264 px at the design width; check the sticky column, its edge shadow, `scroll-padding-inline-start` and the focus ring all still line up against the new width, and that the page still never scrolls horizontally. Deep names may now truncate at 375 — that is expected and FR5 already covers it. **[Agent: react-frontend]** _(Done 2026-09-22: one `--table-name-col-w` override on `.scroller` under `@media (width < 600px)`, via a new `--table-name-col-w-narrow: 160px` token; the sticky cell, the table's minimum width and `scroll-padding-inline-start` all read it, and the focus ring, edge shadow and reveal overlay sit inside that cell; 264 px unchanged at the design width. lint + typecheck + 155 unit tests green. **Not measured in a browser**: no browser could start in that pane (`mach_port_rendezvous_mac.cc … (141)`, the machine's stale bootstrap namespace after the restart — the same wall the testing lane hit), so the e2e half of the gate did not run. A measurement spec for 375/599/600/1440 is waiting in the lane worktree as `zz-measure-name-col.spec.ts`. Commit f54e555, merged 5d6edd9.)_
  - [x] **Defect found by the acceptance suite — the revealed name paints no background.** When a long name is revealed on hover or keyboard focus its box is 0 px tall, so the month figure behind it shows through and reads as "Anna Blackwood25" (screenshot in the lane's report; 5 tests red at both widths, `table-truncation.spec.ts:52`, `:83`, `:124`). The testing lane found a fix that makes all 7 pass and reverted it rather than edit your files: `.nameSlot { align-self: stretch; min-block-size: 1lh }` in `apps/web/src/widgets/clients-table/ui/clients-table.module.css`. Apply it or something better, and check the reveal still sits above the month cells and inside the sticky column at 375. FR5 is amended to say a revealed name covers what is behind it. **[Agent: react-frontend]** _(Done 2026-09-22: applied the testing lane's fix, RED-proven first (5 failing before, 7/7 after) and checked in the browser at 375: the revealed "Anna Blackwood" covers the "25" behind it, and 10 px past the sticky edge the topmost element is still the label. Root cause found: the label goes position:absolute on reveal and a branch row's only other flex item is the 16 px chevron, so the slot collapsed; the 1lh minimum keeps it a line tall. Commit 7f6347a)_
  - [x] **Defect found by the WebKit pass — a collapse leaves a row behind in Safari's engine.** In WebKit only, and every time (3/3 with `--repeat-each=3 --workers=1`), `@formkit/auto-animate@0.10.0` throws `TypeError: undefined is not an object (evaluating 'parentCoords.top')` at `deletePosition` (`index.mjs:705`) on collapse; the departing "Anna Blackwood" row then stays in the DOM **and is not inert**, so the table shows 5 rows where 4 are expected. Four specs fail there: FR2-AC2, FR2-AC3, FR3-AC14 and FR2-AC8 (rapid toggle). Diagnose whether our `rowMotion` plugin's use of the coordinate arguments triggers it, and fix it in our code if so (guarding the plugin, or not returning keyframes when a parent's coordinates are missing); if it is purely the library's bug, say so with the evidence and propose the smallest safe option. **This blocks the owner's Safari + VoiceOver check**, so it comes before the final device pass. **[Agent: react-frontend]** _(Done 2026-09-22: fixed with one line — `.table tbody { position: sticky }` — and it is **not our plugin**: WebKit computes `position: relative` on a <tbody> as `static` while Chromium keeps it, so the library looks up a position it never recorded and gets undefined; the throw lands before the row is marked inert, which is why the row stayed. It only happens when a collapse interrupts a slide still running. WebKit does honour `sticky` on a tbody, and sticky with no offsets moves nothing. Before: 4 specs red in WebKit; after: every table spec passes with --repeat-each=3 --workers=1, no TypeError, and in both engines a leaving row stays exactly where it stood (top 698, name cell pinned at 16 px, also scrolled sideways at 375). Commit d378512 — the owner's Safari + VoiceOver check is no longer blocked)_
  - [ ] Merge `lane/002-s5-testing-expert`, run `pnpm check`, push, confirm CI green on the PR, record ledger notes, `swarm.sh clean 002`. **[Lead]**
  - [ ] Verify — device: repeat the VoiceOver pass from slice 3 on the finished table, at desktop width and at 375 px, including a collapse while focus is deep inside a branch. Confirm nothing is announced from rows that have just disappeared. **[User]**
