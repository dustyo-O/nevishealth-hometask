# Tasks — 004 The Data As Supplied, And An Uneven Company

Spec: `functional-spec.md` (FR1–FR6, 27 acceptance criteria) · How: `technical-considerations.md` (§1–§4) · Reviews: `reviews/TRIAGE.md` (spec review F1, F2) · Grill: `context/inbox/supplied-payload-non-uniform-nesting.md` (D1–D19).

**Read the slice order before starting.** Slice 1 teaches the chart to account for unattributed clients while the data is still complete, so nothing changes on screen. Slice 2 swaps the data, and the remainder appears the same moment the gaps do. Built the other way round, the app would sit between slices with a chart showing a tenth of the table's figures — the exact contradiction this spec exists to remove.

## Standing rules

- Read `CLAUDE.md`, then `functional-spec.md`, then `technical-considerations.md`, then the grill notes. The tech doc's numbers were measured, not guessed. Do not "improve" a value without re-measuring and saying so in your ledger note.
- `type` aliases over `interface`, everywhere. CSS Modules with tokens. FSD import direction downward only; every slice exposes `index.ts`.
- `recharts` stays imported in exactly one file, `ui/bar-plot.tsx`.
- **Do not count bar segments in any test.** A zero-valued channel may render no rectangle — the real payload drew 31 where the current tests expect 36. Assert per month and per series, by colour and value.
- **Never make the data uniform again.** The gaps are the requirement. If a test is awkward because a branch has no advisers, the test changes, not the data.
- Tests are written with the change and proven RED first (temporarily revert, watch it fail, restore). Never delete or skip a test to make a gate green. A completion claim cites fresh output from this run.
- Gate before you finish: your lane's gate from `harness.json`. Not green → `PARTIAL`, with the failure quoted. The e2e server is on **5273**; if you start a dev server for your own exploration, use another port.
- Every finished sub-task ends with `_(Done <date>: what actually happened, incl. surprises)_`.

---

- [x] **Slice 1: The chart can account for clients nobody attributed**

  > All of the chart work, while the served data is still complete — so `shown` is false throughout and **nothing changes on screen**. Everything here is proven against a routed fixture with deliberate gaps (FR3, FR4, FR5, FR6).
  - [x] `entities/clients/model/monthly-series.ts`: make sure the series carries the **company's own monthly figures** alongside the channel series, since the remainder is company minus channels. The entity gains nothing about "Not recorded" — it reports what the data says, and the data does not say this. Tests for the company totals surviving the mapping. **[Agent: react-frontend]** _(Done 2026-09-23: `MonthlyPoint` gained `company` — the node's own figure as served, never reconciled — while `total` still means the sum of recorded channels. Nothing about "Not recorded" entered the entity. 2 new tests, RED first. Surprise: every hand-built `MonthlyPoint` in `describe-month.test.ts` and `y-scale.test.ts` needed the new field before it would typecheck.)_
  - [x] `widgets/clients-chart/lib/not-recorded.ts` per §2.3: `notRecorded(series)` → `{ values, shown }`, where `values[m] = max(0, total[m] − Σ channels[m])` and **`shown` is `values.some(v => v > 0)` — one decision for the whole series, never per month** (spec review F2). Pure, no Recharts, no DOM. Tests RED first: a complete series gives `shown: false`; a series with gaps gives the right figures; a month whose channels exceed the total gives 0, not a negative (spec review F1); `shown` stays true when only one month has a remainder. **[Agent: react-frontend]** _(Done 2026-09-23: `notRecorded(series)` → `{ values, shown }` with `max(0, company − Σ channels)` and **one `shown` across the whole year**; `withNotRecorded(series)` returns the same object when `shown` is false, otherwise puts Not recorded first in every month — zeros included — and adds it into the total, so a bar is the Company figure or, on overshoot, the channel sum. 10 tests: RED when the module was missing, then 6 RED against a deliberately broken version with no floor and `shown` decided month by month.)_
  - [x] Add `--color-channel-not-recorded` to `shared/styles/tokens.css` and map it in `widgets/clients-chart/model/channels.ts`. It must read as **absence, not a fourth channel**: no hue, clearly distinct from the three channel colours, light enough that ~90% of a bar in it does not overwhelm the colours above. The existing greys were chosen for hairlines — pick the mix and **check it in a browser against the card**. `channels.ts` must keep throwing on a genuinely unknown name. **[Agent: react-frontend]** _(Done 2026-09-23: the token is `color-mix(in srgb, var(--color-text) 12%, var(--color-surface))` ≈ rgb(227,227,227) — checked in Chromium at 1440 against the white card, where it reads as absence under the colours and stays visibly apart from the peach New organic and from the 4 % hover tint. `channelKey` maps 'Not recorded' and still throws on Referral, Unattributed and 'Not Recorded' (RED first). Swatch classes added to the legend and panel modules.)_
  - [x] Render the not-recorded `Bar` **first** in the stack so it sits at the base with the three channels above in the design's order (FR3), and give the legend, the month panel, the hidden data table and the announcement the single `shown` flag (FR4, FR5). When `shown` is true every month lists the segment, **including months where it is zero**; when false, none of them mention it. **[Agent: react-frontend]** _(Done 2026-09-23: the widget builds `withNotRecorded(toMonthlySeries(data))` **once**, and the drawing, legend, panel, hidden table and live region all read that one series — so they share the flag by construction and `bar-plot.tsx` did not change at all. 8 component tests on a gapped fixture, 6 of them RED before wiring. The segment tests group by column and never count rectangles, per the standing rule.)_
  - [x] Verify — browser, with a routed fixture whose channels cover only part of the company: the bar heights still equal the Company row, the base segment is the neutral grey, the legend names four entries, a zero month still lists "Not recorded" reading 0, and the legend does not change while the pointer moves across months. Then load the **current** complete data and confirm **nothing has changed** — three segments, three legend entries, no zero line anywhere. Delete the fixture route and any screenshots. **[Agent: react-frontend]** _(Done 2026-09-23: Vite on 5391 with the API faked to a gapped fixture (only Anna keeps channels; June's company figure set to her 32 to force the overshoot). Bar heights `[250,267,284,301,32,334,350,250,250,250,250,350]` match the Company row exactly, the base is grey everywhere except June where there is nothing to draw, Feb reads 225/25, the legend has 4 entries and did not change across 12 hovers, June's panel reads "Not recorded 0 … Total 32". On the complete data: 12 rectangles per channel colour, a 3-entry legend, no "Not recorded" anywhere in the page, announcement unchanged — **nothing changed on screen**, which was the point of the slice. Route, fixtures and screenshot deleted.)_
  - [x] Merge the lane, run `pnpm check`, push, confirm CI green, record ledger notes, `swarm.sh clean 004`. **[Lead]** _(Done 2026-09-23: merged --no-ff; `pnpm check` green on the merged tree — 243 unit across 27 files, 154 e2e.)_

- [x] **Slice 2: Serve the payload the brief actually supplied**

  > The swap (FR1, FR2). **One lane, `[Lane: shared]`** — the directories look separable but the dependency is not: the web's expectations come from `shipped-clients.ts`, which reads the API's data file, so the swap and the expectations that follow it must land in one tree or the lane's own gate is red. `developer` owns `./` and runs alone, which is what this needs. Its gate is `pnpm check`.
  >
  > The chart still stacks channels here and **will look wrong on the real data** — bars at a tenth of the table's figures. That is expected and slice 3 fixes it; do not "improve" the chart in this slice.
  - [x] Replace `apps/api/src/clients/data/clients.json` with the payload exactly as the brief supplies it (it is `context/inbox/data.original.json` today). Make the inbox copy match and **delete `data.original.json`** — with the completed variant gone it is a duplicate, and git history holds what we discarded. Change no figure, including the ones that do not add up. **[Lane: shared]** **[Agent: developer]** _(Done 2026-09-23: `clients.json` and `context/inbox/data.json` are byte-identical copies of the supplied payload (12 nodes); `data.original.json` deleted. Surprise: `brief.md` pointed at the deleted file, so its heading now points at `data.json` and its "Data adjustments" section carries a one-paragraph "Reversed by spec 004" note. The section itself was not rewritten.)_ _(Done 2026-09-23: `clients.json` and `context/inbox/data.json` are byte-identical copies of the supplied payload — 12 nodes — and `data.original.json` is deleted. Surprise: `brief.md` pointed at the deleted file, so its heading now points at `data.json` and its "Data adjustments" section carries a short "Reversed by spec 004" note.)_
  - [x] The API's assertions, per §2.2 — no production code changes, the guard already does the right thing. `scripts/smoke.mjs` asserts `0 discrepancies`; it must assert **exactly seven** and print them. `consistency.spec.ts` asserts `Checked 44 nodes`; the tree is now **12**. Add a test pinning all seven by path, month, expected and actual — Company May (301 vs 279), Branch 1 Aug (214 vs 216), Anna May (31 vs 30), Jun (32 vs 33), Jul (34 vs 35), Aug (38 vs 36), Sep (27 vs 28). **[Lane: shared]** **[Agent: developer]** _(Done 2026-09-23: no production change. `consistency.spec.ts` pins the seven, in depth-first order. The break-one-figure tests moved to a small in-file consistent tree with a leaf at every level, because the shipped data no longer adds up. `smoke.mjs` asserts `Checked 12 nodes: 7 discrepancies` and that the boot log names exactly the seven, then prints them. Surprise: Nest colours its log, so the smoke strips the colour codes with `util.stripVTControlCharacters` before comparing. The e2e and acceptance tests now expect 12 nodes and seven warnings, and the data is still served. 001's FR2-AC5 ("no data warnings") is now superseded by 004 FR2; the acceptance test says so, and the 001 amendment is slice 4's. RED shown against the old completed data (7 fail) and against one changed figure (5 fail).)_ _(Done 2026-09-23: **no production code changed.** The smoke script expects `Checked 12 nodes: 7 discrepancies`, checks the boot log names exactly those seven and prints them; the seven are pinned by path, month, expected and actual. The unit tests that break one figure moved to a small consistent tree, since the shipped data no longer adds up. RED proven twice: 7 fail against the old data, 5 against one changed figure. **Found for slice 4:** spec 001's FR2-AC5 ("no data warnings") is overruled by 004 FR2 and still needs its amendment.)_
  - [x] The web's expectations follow the data (§2.7). `shipped-clients.ts` reads the served file so it follows automatically, but everything derived from the completed tree changes. Give the **e2e double a non-uniform shape** so the acceptance tests exercise leaves. Replace any segment-count assertion with per-month, per-series checks. Where a chart expectation is about to be replaced wholesale by slice 3, mark it rather than perfecting it. **[Lane: shared]** **[Agent: developer]** _(Done 2026-09-23: the e2e double's default company is now uneven: only Branch 1 has advisers, and only its Adviser 1 has a channel. A new `loaded.spec` test walks it. `openChart` now waits for twelve columns instead of 36 rectangles, and every chart reading is per month and per series. Channel-model expectations (Feb 225 / 25 / 0 / 0 = 250) are marked `004 slice 3 replaces`. `reshaped()` now shifts the ancestors by the channel's delta instead of re-summing them, so the seven discrepancies survive in its copies. Surprises: (1) at a 0–500 scale, rounding bar centres split a column in two, so `readDrawing` now groups centres that fall within 1 px; (2) the not-recorded fill computes to `color(srgb 0.889412 0.889412 0.888941)`, not an `rgb()`; (3) table specs that opened Branch 2 or 3 now use Anna Blackwood. 002 FR2-AC4 became "opening another row leaves Branch 1 open; clicking Branch 2 opens nothing", and its wording is left to slice 4; (4) FR3-AC18 cannot cut a row off at the top of an 812 px screen with 12 rows (measured: the page scrolls at most 474 px and the table starts at 590), so it runs at 375×480 on row 3. The data was not padded. RED shown against the old data and the old uniform double (8 fail). `pnpm check` is green: 18 + 71 unit tests plus the smoke, 243 web unit, 155 e2e.)_ _(Done 2026-09-23: the e2e double's default company is now uneven — only Branch 1 has advisers, only Adviser 1 has a channel. No test counts segments; they check each month and each series, and chart expectations slice 3 will replace are marked `004 slice 3 replaces`. Surprises: rounding split a bar in two at the 0–500 scale, so bars are grouped by position within 1 px; and **FR3-AC18 cannot cut a row off at the top of an 812 px screen with only 12 rows** (measured), so it now runs at 375×480 rather than padding the data to suit the test. Table tests that opened Branch 2 or 3 now use Anna Blackwood, with 002 FR2-AC4's wording left for slice 4. 8 changed tests failed as expected against the old data and the old uniform double.)_
  - [x] Verify — browser: the table shows Branch 2 and Branch 3 with no control to open them and only Anna expandable inside Branch 1; the Company row reads **301** for May above branches totalling 279, with no warning anywhere; the API's boot log reports seven discrepancies and serves the data anyway. Delete the check's artifacts. **[Lane: shared]** **[Agent: developer]** _(Done 2026-09-23: built API on :3391 and Vite on :5391 through a temporary proxy config, so no other checkout was involved. Boot log: seven WARN lines, then `Checked 12 nodes: 7 discrepancies`, and `/api/clients` answered through the proxy. In Chromium at load: Company May 301 over branches 156 + 87 + 36 = 279. Branch 2 and Branch 3 have no chevron and no `aria-expanded`, and clicking Branch 2 changed nothing. With Branch 1 open, only Anna Blackwood has a chevron, and clicking James Walker changed nothing. No alert, and no warning text anywhere on the page. Servers stopped; the temporary config and the MCP snapshots were deleted.)_ _(Done 2026-09-23: the built API on :3391 logged seven warnings plus the count line and served the data anyway. In the browser the Company row reads **301** for May above branches of 156 + 87 + 36 = 279; Branch 2 and Branch 3 show no control to open them and only Anna opens inside Branch 1; clicking Branch 2 or James Walker changed nothing; no warning anywhere on the page. Servers stopped, temporary config and snapshots deleted.)_
  - [x] Merge the lane, run `pnpm check`, push, confirm CI green, record ledger notes, `swarm.sh clean 004`. **[Lead]** _(Done 2026-09-23: merged --no-ff; `pnpm check` green on the merged tree — 18 contracts, 71 API tests plus the production smoke, 243 web unit, **155 e2e**. 27 files, +569/−1757.)_

- [ ] **Slice 3: Existing clients absorbs what the data does not record**

  > The chart model, settled by the design (spec Change Log, 2026-09-23). Much smaller than the two models it replaces: one number is redefined and slice 1's fourth segment is removed (FR3, FR4, FR5).
  - [x] Redefine the three series per §2.3: `newOrganic` and `newPaid` stay summed from the tree; **`existing[m] = company[m] − newOrganic[m] − newPaid[m]`**, clamped at zero. **(measured)** Existing becomes `250, 266, 282, 299, 315, 331, 348, 247, 248, 248, 248, 346` and the three sum to the Company row in all twelve months. Slice 1's `notRecorded` computed exactly the amount Existing now absorbs — keep the function and its tests, rename it for the job it now does, and prove the sum with tests RED first. **[Agent: react-frontend]** _(Done 2026-09-23: `lib/not-recorded.ts` → `lib/existing-clients.ts`; `existingClients()` = `max(0, company − New organic − New paid)` and `withExistingClients()` replaces `withNotRecorded()`. Existing reads `250, 266, 282, 299, 315, 331, 348, 247, 248, 248, 248, 346` and sums to the Company row in all twelve months. RED against **two** alternatives — slice 1's "recorded Existing only" (8 failed) and "recorded + remainder" (2 failed). Surprise worth keeping: the second alternative matches the supplied data exactly and only diverges once recorded channels overshoot the company, so a test now covers that difference rather than leaving the two formulas indistinguishable.)_
  - [x] **Remove the fourth segment and everything it drove**, so nothing dormant is left for a component review to find later: `withNotRecorded`'s segment-adding behaviour, `--color-channel-not-recorded` and its swatch classes, the `shown` flag and every conditional it drives in the legend, panel, hidden table and announcement, and the `'Not recorded'` entry in `channels.ts` — whose throw on unknown names stays, because the three channel names are data again. The gate must prove nothing references them. **[Agent: react-frontend]** _(Done 2026-09-23: removed the token, both `.not-recorded` swatch classes, the `'Not recorded'` entry in `channels.ts` (its throw stays, with a test that 'Not recorded' now throws), the `shown` flag and every stale comment; the e2e suite is back to three parts. New `e2e/chart-no-fourth-part.spec.ts` sweeps every non-test source file on disk **plus** the served page's text, markup and stylesheets for any trace, shown RED by putting the old token back. Surprise: it could not be a Vitest test — `src` has no Node types, and a CSS Module imported with `?raw` still comes back as a class-name proxy under Vitest.)_
  - [x] `minPointSize` on the bars as a **function** so zero stays zero — `(value) => (value > 0 ? 2 : 0)` — because the newly acquired are 0–2 clients a month, under two pixels drawn to scale (FR4). Then widen the pixel-reading assertions in the acceptance suite to tolerate the floor, **and record beside the tolerance why it exists**, so nobody later tightens it back and breaks the suite. Figures stay asserted exactly; only heights get the tolerance. **[Agent: react-frontend]** _(Done 2026-09-23: **the tech doc's formula was wrong and the lane proved it.** Recharts 3.10 passes `minPointSize` the running top of the stack, not the part's own value, so `(value) => value > 0 ? 2 : 0` drew February's zero parts at 2 px and failed FR4-AC2 — measured in jsdom and Chromium. Shipped `floorOf(points, name)`, which looks the part's own figure up by index, with the reason commented at the call site. Figures are asserted exactly from the hidden table and the served tree; heights use `expectBarShows` against `max(to scale, FLOOR_PX)`, and the `FLOOR_PX` comment says why the tolerance must not be tightened. Without the floor, 9 e2e tests fail.)_
  - [x] Verify — browser: twelve bars each in three parts; February reads 250 / 0 / 0 with no new-client parts drawn at all; July reads 331 / 2 / 1 with both new parts visible rather than hairlines; every bar's figures sum to the Company row; the legend names exactly three parts; no "Not recorded" text, token or swatch anywhere in the page. Delete the check's artifacts. **[Agent: react-frontend]** _(Done 2026-09-23: on the lane's own server at :5290. Twelve bars; every hidden-table row reads Existing / New organic / New paid summing to the Company row (250 … 350, matching the treegrid). February's panel reads 250 / 0 / 0 / 250 and the bar draws **only Existing** (189.38 px) — no new-client parts at all. July reads 331 / 2 / 1 / 334 with both new parts drawn at 2 px. At 0.76 px per client every new part in the year is floored. Legend is exactly the three names; no "Not recorded" in text, markup or CSS. **Two conflicts escalated rather than resolved alone:** "each bar in three parts" cannot hold with FR4-AC2 (only 7 of 12 bars have all three; February has one), and when both new parts are floored the upper covers ~0.5 px of the lower. Both are now in the spec's Change Log.)_
  - [x] Merge the lane, run `pnpm check`, push, confirm CI green, record ledger notes, `swarm.sh clean 004`. **[Lead]** _(Done 2026-09-23: merged --no-ff; `pnpm check` green on the merged tree — **159 e2e**, up from 155. The lead resolved both escalations: FR3-AC1 now describes the order of the parts present rather than promising three, and FR4 records the half-pixel overlap as accepted with its reason.)_
  - [x] Verify — look and feel: the bars will read as almost solid, because this company records almost no new acquisitions — 0–2 clients a month against 250 or more. Judge whether that is acceptable as the honest picture, knowing the mockup's proportions cannot be reproduced from the supplied figures (R-1). **[User]** _(Done 2026-09-23: owner looked at the built chart and gave a verdict — "new organic + new paid bars are really thin" — and asked for a larger minimum height. The solid-looking bars were accepted as the honest picture; it is the two-pixel floor that was rejected. Measuring July then showed the floor could never fix New organic at any size, because the part above it starts at its true offset and covers the difference. FR4 is amended: four pixels, borrowed from the existing-clients part so the bar's total stays exactly the Company row. Slice 3b implements it.)_

- [ ] **Slice 3b: Both newly-acquired parts are actually visible**

  > FR4, amended after the owner saw the chart. Slice 3's `minPointSize` floors the drawing but leaves the stack offsets on the true figures, so every lifted part except the topmost is covered by its neighbour — measured on July, New organic drew 2 px and showed 1.51 px.
  - [ ] Replace `minPointSize` and `floorOf` with a floor applied to **the values the drawing is built from**, per §2.4: each part with clients in it is raised to at least four pixels' worth, and the total raised is **subtracted from the existing-clients part of the same bar**, so the bar's height still equals the Company row exactly. Express the floor as a share of the axis domain, not a fixed number of clients, and **measure the resulting pixel height in a browser** rather than trusting today's 0.757 px per client. Guard the degenerate case where the borrowed total would exceed the existing part: the floor gives way rather than driving a part negative. **[Agent: react-frontend]**
  - [ ] Keep the true series and the drawn series **visibly separate** in the code — the panel, the hidden table and the announcement read the true one, and nothing in a later tidy-up should be able to merge them. Tests RED first: the drawn series lifts and borrows; the read series does not; a month with no newly acquired clients is identical in both. **[Agent: react-frontend]**
  - [ ] Tighten the acceptance suite where it can now be tight: the **bar total** is exact again, so its tolerance returns to what it was before slice 3 widened it, while per-part heights keep slack for the floor. Update the `FLOOR_PX` comment to say the total is exact and only the parts give way. **[Agent: react-frontend]**
  - [ ] Verify — browser: July draws New organic and New paid each at least 4 px with neither covering the other, and its bar's total height equals the Company row; February draws Existing alone, exactly to its figure; every month's panel and hidden table still read the true figures. Report the measured pixel heights of both new parts. Delete the check's artifacts. **[Agent: react-frontend]**
  - [ ] Merge the lane, run `pnpm check`, push, confirm CI green, record ledger notes, `swarm.sh clean 004`. **[Lead]**
  - [ ] Verify — look and feel: judge whether 4 px reads well enough now that both parts are genuinely visible, or whether it wants more. Changing it is one number. **[User]**


- [ ] **Slice 4: The record catches up**

  > The documents that now describe behaviour the app no longer has. Audited in §2.6, not guessed.
  - [ ] Amend **spec 002** in place with a Change Log entry: rewrite "Given Branch 1 is open, when the user opens Branch 2, then Branch 1 stays open" against a row that still has children, and add one criterion asserting a childless branch offers nothing to open. Note that expansion state moved to a shared store without changing any behaviour it specifies. It keeps its `Completed` status. **[Lead]**
  - [ ] Amend **spec 003** in place with a Change Log entry: its chart is unchanged in shape — company-wide, three stacked channels, three-entry legend — but Existing clients is now derived from the company's figure rather than summed from the tree, and small parts have a minimum drawn height. Only the example figures move: FR1-AC3, FR4-AC1 and FR6-AC1 name February as 221 / 15 / 14, which becomes 250 / 0 / 0. FR1-AC4 still holds. It keeps its `Completed` status. **[Lead]**
  - [ ] Amend **spec 001**'s Change Log: the data decision it records — completing the payload — is reversed, with the reason. **Also found by the slice 2 lane:** 001's FR2-AC5 promises the user is never warned about the data, which 004 FR2 now overrules in wording though not in behaviour — amend it to point at 004 rather than leaving two documents making the same promise differently. Then `product-definition.md` §4 ("we completed the data rather than change the chart"), which reverses, and its open question about what the chart stacks, which the design has now answered. The roadmap is unchanged: "Chart Follows the Drill-Down" stays a Phase 2 item. **[Lead]**

- [ ] **Slice 5: Feature Testing & Regression**

  > Verifies the whole feature end-to-end against functional-spec.md, run after all implementation slices are complete.
  - [ ] Read functional-spec.md acceptance criteria in full. Generate acceptance-level tests that verify the entire feature as a whole — not individual slices. Cover applicable layers (unit for pure logic, integration for service interactions, e2e for user flows) based on the project's testing stack. Write tests with RED validation (must fail before implementation is confirmed done). Annotate each test with `@spec: 004-supplied-payload-non-uniform-nesting` and `@regression` if suitable for long-term regression. **[Agent: testing-expert]**
  - [ ] Run all generated tests. All must pass. Fix any failures before proceeding. **[Agent: testing-expert]**
  - [ ] **The regression this spec exists to prevent** (§4): a test that fails if the served data is ever made uniform again — read the served file and assert Branch 2 has no children and at least one adviser has none. Without it, a future tidy-up quietly re-creates the mistake this spec corrects and every other test still passes. **[Agent: testing-expert]**
  - [ ] **The agreement test, against the supplied figures:** for each of the twelve months the chart's three figures must sum to the Company row's. It reads the figures from the panel and the hidden table, which are exact, rather than from pixels, which now carry the `minPointSize` floor. Then re-run spec 003's swept label test and the axis assertions (R-4), and run the chart and table specs in **WebKit** as well as Chromium. **[Agent: testing-expert]**
  - [ ] Merge the lane, run `pnpm check`, push, confirm CI green on the PR, record ledger notes, `swarm.sh clean 004`. **[Lead]**
