# Functional Specification: Component Review

- **Roadmap Item:** Phase 1 — Component Review, the last thing before the work is sent: read every component as a reviewer would, split what does two jobs, lift complex logic into hooks, and remove what nothing uses.
- **Status:** Completed
- **Author:** Alexander Shleyko
- **Sources:** the four parallel review reports in `context/inbox/consults/react-frontend-component-review-*.md` (29 findings), `context/product/roadmap.md`, and specs 001–004, whose behaviour this work must leave untouched

---

## 1. Overview and Rationale (The "Why")

This one changes nothing for the person using the dashboard. That is the point of it.

The brief asks for component APIs designed "the way you would on a real team: composable, with clear boundaries", and says so as a thing it will judge. Duplication and wrong seams are only visible once everything exists, which is why this is the last item rather than the first: the table and the chart both had to be built before anyone could see what they share, what leaked between them, and what was written twice.

Four reviewers read the code in parallel, each a different slice, against four questions: is it concise and does it do one thing; does anything render *and* decide; is anything in `shared/` actually used once; and is intricate logic sitting in a component when it should be a hook that can be read and tested alone. They reported 29 findings, including one test that had silently guarded nothing since a rename.

Success is measured by what does **not** happen: every acceptance criterion in specs 001–004 still passes, unedited, and nobody using the dashboard can tell this work was done.

---

## 2. Functional Requirements (The "What")

### FR1 — Nothing the user can see changes

Every behaviour specified by specs 001, 002, 003 and 004 survives exactly: the table's levels and rows that open, the whole keyboard model, what a screen reader is told, the chart's three parts and their figures, the panel, the narrow-screen layout, and the loading, failed and retry states.

- **Acceptance Criteria:**
  - [x] When the full check runs after this work, then every test written for specs 001–004 passes **without having been edited**, except where a review finding says a test itself was wrong and the ledger records which and why. _(Verified 2026-09-23 by `pnpm check` on CI for PR #7 — the same command the harness gate runs — reporting **pass**: prettier, then contracts and API (lint, typecheck, unit, e2e, `nest build`, the `NODE_ENV=production` boot smoke), then web (lint, typecheck, 281 unit tests across 29 files, Playwright against the dev server and against a production build). The eleven test files that changed are each named in the ledger with the finding that required them; the one test that was itself wrong is chart F1, the refetch test that had asserted nothing since a prop rename.)_
  - [x] When the user opens the dashboard at 1440 px and at 375 px, then the table and the chart look and behave as they did before this work. _(Verified 2026-09-23 in the browser, on the lane's own `vite --port 5174`. 1440: no sideways scroll, name column 264 px, rows 56, indents 16/44, one tab stop; Down/Enter/Down/Right lands on Anna's first figure by its header ids, and closing Branch 1 returns focus to Branch 1. 375: name column 160, every figure lands exactly at the sticky edge walking left, the reveal capped at 183 px of visible room. `?delay=2500` silent then announced at 1 s, placeholders the same 280 px as the table; `?fail=1` panel with the 500 detail at both widths, Retry focuses the heading, every request carried the switch.)_
  - [x] When a test had to change, then the ledger says which finding required it and what was wrong with it. _(Verified 2026-09-23 by diffing every test file against the branch point: eleven changed, each named in the ledger with its finding. `summaries.test.ts` deleted with its function (app F1); `query-client.test.ts` lost a test asserting two constants equal themselves (shared F9); `keyboard.test.ts`'s helper follows `reduceKey` losing its dead `expandedIds` (shared F4); `tree-grid.test.tsx` and `tree-grid-motion.test.tsx` follow `name` → `nameColumn` (shared F8); `drawn-series.test.ts` has one **title** naming the deleted `--chart-plot-h` token; `clients-chart.test.tsx`, `clients-chart-refetch.test.tsx` and `clients-table.test.tsx` follow the widgets taking `data` (table F1 + app F3); `clients-service.e2e-spec.ts` loses a fixture property (app F5); `use-delayed-flag.test.ts` is new (app F4). **The one that was itself wrong is chart F1** — the refetch test's mock read `series` after the prop had been renamed to `drawing`, so it asserted nothing; it is now typed against `ComponentProps<typeof BarPlot>` and was proven RED.)_

### FR2 — A reviewer meeting the code reads the code first

The intent behind a decision stays; the history of how it was measured moves to the ledger that already keeps such things. What a file says about itself matches what it does, and the "generic" layer does not name the business it is generic over.

- **Acceptance Criteria:**
  - [x] When someone greps the shared layer for the words of this business — "month", "Company", "clients" — then the hits are in that layer's own parameters and not in its rules or its comments. _(Verified 2026-09-23 by grep: `shared/ui`, `shared/lib` and `shared/api` return **no** hit for month/company/clients/adviser outside tests — the only match is the word "client" inside `createQueryClient`. The tree grid now reads nine `--tree-grid-*` parameters with defaults, listed at the top of its stylesheet, and the widget sets them. **Stated honestly:** `shared/styles/tokens.css` still says "month" and "clients chart" in its comments and in `--table-month-col-min-w`. That file is the app's design-token sheet — the values the design chose — not a generic component's rules, so it is left as it is rather than renamed to hide what it is for.)_
  - [x] When a document describes a signature or a file, then that signature or file exists as described, or the document is amended in the same commit. _(Done across all three slices: 002 §2.1 with `reduceKey` and the tree-grid index (which had diverged in two further ways the report found), 002 §2.1 again for `gridProps` and `nameColumn`, 002 §2.5 for the nine grid parameters, 003 for `formatBranchCount` and `PLOT_HEIGHT`, 001 §2 for `summaries.ts`, the entity index and `loadConfig`, and `architecture.md` §6, whose "widgets are the only place the two meet" now says "at runtime" and names the type-level exception. Two 001 rows the lanes' greps did not reach — D-12's `formatBranchCount` assumption and the web-unit test row listing its cases — were amended by the lead on merge. The nest-backend lane could not edit `context/`, so it reported `BLOCKED` and named the line rather than diverging silently. **This criterion was first ticked on weaker evidence than it deserved** — against the amendments the lanes had made, not against a search for documented files that do not exist. The search was then run over every `technical-considerations.md` and `architecture.md`: of eight paths that resolve to nothing, seven are rows that say the file was deleted or moved and name where it went. The eighth was real — 003 §2.1's `month-ticks.ts` / `sparseMonthIndices`, a file never written, which our own reviewer had reported as chart F8 and no slice had picked up. It is fixed, and the finding is triaged in `tasks.md`.)_
  - [x] When a public export list is read, then everything on it is imported by something outside its slice. _(Verified 2026-09-23 by resolving every name on every `index.ts` against the rest of `src/`: `entities/clients`, `shared/api`, `shared/ui/tree-grid` and both widget indexes have no name left that nothing outside their slice imports. Seven names went from the entity alone — `CLIENTS_PATH`, `buildClientsUrl`, `fetchClients`, `FetchClientsOptions`, `ClientKind`, `formatBranchCount` and `DevSwitches`, the last of which the report itself missed.)_
