# Tasks — 005 Component Review

The last roadmap item before the work is sent. **No functional change anywhere**: every acceptance criterion in specs 001–004 must still pass, unedited, at the end of this. If a change would alter behaviour, it is the wrong change.

Four reviewers read the codebase in parallel and reported 29 findings; the reports are the contract for this work and are quoted by id in every task:

- `context/inbox/consults/react-frontend-component-review-shared-20260923-184320.md`
- `context/inbox/consults/react-frontend-component-review-table-20260923-184320.md`
- `context/inbox/consults/react-frontend-component-review-chart-20260923-184321.md`
- `context/inbox/consults/react-frontend-component-review-app-20260923-184321.md`

## Standing rules

- **Read your slice's report first**, in full. Each finding names files and lines, says what a reader struggles with, and estimates the cost. Where a finding proposes a seam, take the seam it proposes unless you can say why a better one exists.
- **Behaviour does not change.** The gate is the proof: every existing test must pass **unedited**, except where a task explicitly says a test is wrong. If you find yourself editing a test to make it pass, stop and report it.
- `type` aliases over `interface`. FSD import direction downward only. `recharts` stays imported in one file.
- Where a finding contradicts a spec or tech doc, **the document is amended in the same commit** — never silently diverged from. Say so in your ledger note.
- Gate before you finish: `pnpm check:web` (or `pnpm check` for the API lane). Not green → `PARTIAL` with the failure quoted.
- Every finished sub-task ends with `_(Done <date>: what actually happened, incl. surprises)_`.

---

- [x] **Slice 1: The two big extractions, and the dead weight around them**

  > Two lanes, no shared files: one works only inside `shared/`, the other only inside `widgets/clients-chart/`.
  - [x] **shared F1 — split `useTreeGrid` (220 lines, five jobs).** Per the report's seam: `useRevealOnOpen(ids, rows, expandedIds)` returning `markOpening(id)` takes lines 129-149 plus the `openingRef` line in `toggle`; `useFocusCursor(ids, cursor, rows, hasMovedRef)` takes 95-124. Cursor, keys and focus-follow stay, about 90 lines. **The layout-effect order is load-bearing** — focus-scroll must still run before reveal-scroll, so call the extracted hooks in that order. Also extract `useRowMotion(ref)` next to `row-motion.ts` so `TreeGrid` is markup (report's second bullet). Re-run the **WebKit** e2e afterwards, not only Chromium. **[Lane: shared]** **[Agent: react-frontend]** _(Done 2026-09-23: `useTreeGrid` now calls `useFocusCursor` then `useRevealOnOpen` **in that order**, the ordering the brief flagged as load-bearing; `useRevealOnOpen` returns `markOpening`, which takes `string | null` so closing a row clears a pending reveal — what the old `openingRef` line did. `useRowMotion(ref)` sits beside `row-motion.ts`, leaving `TreeGrid` as markup. **No test was edited.** WebKit 156/156. Surprise: the hook is 158 lines rather than the estimated 90, because its option types and doc comments stay with it.)_
  - [x] **shared F3 — the comments are a changelog.** Keep every rule and every decision id (`D-7`, `D-15a`, the WebKit `tbody { position: sticky }` note — that one must survive in some form or the next person reverts it). Move the dated measurement stories into the owning spec's `tasks.md`, where the ledger already keeps them. Named in the report: `use-tree-grid.ts:108-118`, `tree-grid.tsx`, `row-motion.ts:46-60`, `tree-grid.module.css:64-73` and `:83-90`, `tree-grid-row.tsx:20-28`. **[Lane: shared]** **[Agent: react-frontend]** _(Done 2026-09-23: every rule and decision id kept; the WebKit `tbody { position: sticky }` note survives in shorter form with "do not change it back", which is the point of it. The six dated measurement stories moved to a new section of spec 002's `tasks.md`, where the ledger already keeps such things. Surprise: `prettier --write` reformatted old lines under `context/`, which the formatter is configured to skip — the lane restored them and kept only its new section.)_
  - [x] **shared F4, F6, F7, F9 — the free wins.** Drop the dead `_expandedIds` parameter from `reduceKey` and its call site **and amend spec 002's tech doc §2.1 lines 54-55**, which already diverges from the code in two other ways (the report lists them). Trim `shared/ui/tree-grid/index.ts` and `shared/api/index.ts` to what is imported outside the slice — the report names every unused export. Delete the tautological test at `shared/api/query-client.test.ts:27-30`. Fix the two cosmetic inaccuracies in `avatar` (`hue.ts:3` says unsigned but folds a signed Int32; `avatar.module.css:15` says 20 px where the default is 24). **[Lane: shared]** **[Agent: react-frontend]** _(Done 2026-09-23: `reduceKey` loses `expandedIds`, and **spec 002's tech doc §2.1 is amended in the same commit** — it had diverged in two further ways the report found (the hook takes `id`, not `cellId`; the index list is smaller). `keyboard.test.ts` changed exactly as the finding required. Both `index.ts` files now export only what is imported outside their slice. The tautological test is gone, so 279 unit tests rather than 280. **Surprise worth keeping:** in `hue.ts` the lane fixed the *comment*, not the code — `hash >>> 0` would have given different avatar hues, and nothing the user sees is allowed to change.)_
  - [x] **chart F1 — the refetch test has guarded nothing since 004.** `ui/clients-chart-refetch.test.tsx:11-16` mocks `BarPlot` as `({ series })`, but the prop has been `drawing: DrawnSeries` since commit `8c39d46`, so `series` is always `undefined` and the assertion is true whatever the widget does — **FR7-AC1 is unguarded**. Read `drawing` in the mock, **type the mock against the real component** (`ComponentProps<typeof import('./bar-plot').BarPlot>`) so the next rename fails to compile, prove it RED by dropping the `useMemo` at `clients-chart.tsx:106`, and correct the stale doc comment. **[Lane: chart]** **[Agent: react-frontend]** _(Done 2026-09-23: the mock now reads `drawing`, typed as `ComponentProps<typeof BarPlot>` so the next rename fails to compile rather than silently passing. RED proven: without the `useMemo` it fails "expected 2 to be 1"; restored, it passes and `tsc` is clean. The stale doc comment now names both memos. **FR7-AC1 is guarded again** — it had been guarded by nothing since 004 s4.)_
  - [x] **chart F2 — extract `useMonthReader` from the 239-line frame.** Lines 107-171 plus the announcement become a hook in `model/`, returning `{ index, open, announcement }`, `plotProps` and `drawingProps`. Fold `focused` into the reducer if it comes out cleanly — it already receives `focus` and `blur`. **The listener lifetimes are the risk**: the native `pointerleave` exists because Chromium sends no `pointerout` when the node is replaced, and the document `pointerdown` listener is attached only while open. Keep both, and keep the fast-hover stress test green. Fix the `drawing` shadowing at line 132, and delete the `drawn` flag and the `return null` it protects — the page mounts the widget only when data is present. **Add the hook to 003 §2.1's table in the same commit.** **[Lane: chart]** **[Agent: react-frontend]** _(Done 2026-09-23: `model/use-month-reader.ts` holds the reducer, both native listeners **with their lifetimes unchanged**, the handlers and the announcement; the frame went from 239 to ~140 lines and the `drawing` shadowing is gone. 003 §2.1 gained the hook's row in the same commit. Surprises: `focused` would not fold into the reducer cleanly (`month-reader.test.ts` compares `{index, open}` with `toEqual`), so it stays a `useState` inside the hook; the hook returns `point` rather than `open`, since `open` would go unused; the `drawn` flag and its `return null` became a throw, which TypeScript needs to narrow the type and which the page can never reach; and the plot's three handlers are written out rather than spread, because a spread hid them from `jsx-a11y` and made the documented disable look unused.)_
  - [x] **chart F3, F4, F5, F6 — the rest of the slice.** `initialDimension` is a public prop only tests pass: hide it behind a test-only path or document why it must stay. De-duplicate the channel swatch. Delete the two parameters no caller and no test uses. Make the plot height one number rather than two. **[Lane: chart]** **[Agent: react-frontend]** _(Done 2026-09-23: **`initialDimension` stays**, and the lane was right to keep it — removing it means editing every render in an 845-line suite. Its comment now says it is a test seam and why; `ClientsChartProps` is no longer exported, and nothing imported it. `ui/channel-swatch.tsx` owns the swatch and its `aria-hidden` for both the legend and the panel, duplicate CSS gone. Removed `toDrawing`'s `pxPerClientOf`, `yScale`'s `step`, and the `Size`/`Point`/`ChannelKey` exports. `PLOT_HEIGHT` is the only copy of the plot height, handed to both stylesheets as `--plot-height`, with 003 §2.6 amended to say so.)_
  - [x] Verify — browser: the table and the chart behave exactly as before at 1440 and 375 — open and close rows, arrow through rows and figures, hover and tap a month, check the panel and the hidden table. Nothing in this slice is allowed to change what the user sees. Delete the check's artifacts. **[Lane: shared]** **[Agent: react-frontend]** _(Done 2026-09-23: at 1440 nothing scrolls sideways, Branch 1 and Anna open, the keyboard walk behaves as before, Enter on a leaf does nothing, closing Branch 1 moves focus to Branch 1 and leaves one tab stop, July reads 331/2/1/334. At 375 no sideways page scroll, every figure sits exactly at the sticky column's edge when walking left, tapping May opens the panel at 301, the hidden table has twelve rows. Artifacts deleted.)_
  - [x] Merge both lanes, run `pnpm check` **and** the WebKit pass, push, record ledger notes, `swarm.sh clean 005`. **[Lead]** _(Done 2026-09-23: merged both lanes --no-ff. `pnpm check` green — **279 unit** (one tautological test deleted) and **198 e2e**; the WebKit pass ran clean at **353 passed** across both engines. The lead also removed `--chart-plot-h`, which the chart lane correctly left alone as another lane's file: the token, the test title naming it and the 003 token block are gone, and `drawn-series` still passes 16/16. **Recorded for the harness:** the shared lane's first gate attempt failed and the next two passed unchanged — two web lanes running e2e in parallel clash on the fixed ports 5273 and 4173.)_

- [x] **Slice 2: The boundaries between the page, the widgets and the data**

  > One lane: every finding here touches `clients-table.tsx`, the dashboard page, or both, so splitting it would collide.
  - [x] **table F1 + app F3 — the widget fetches data the page already fetched.** `clients-table.tsx` runs its own query, so it holds two components and a guard that exists only because of that fetch; and "read the switches, then query" is written three times across the page and the widgets. Give the page the data once and pass it down, so each widget takes what it renders. Check spec 001's FR3/FR4 criteria still hold — the loading and failure states are the page's, and must stay the page's. **[Agent: react-frontend]** _(Done 2026-09-23: both widgets take `data: ClientsData`; the page is the only reader of the dev switches and the only query subscriber, `ClientsGrid` and both `data === undefined` guards are gone (the chart's throw included). The page's FR3/FR4 tests pass **unedited**. Tests that had to follow the new signature, because the task changes it: the chart's `renderChart` helper (one function, no assertion touched), the FR7-AC1 refetch test (now renders through a four-line `Host` that plays the page — RED re-proven by dropping the drawing's `useMemo`: "expected 2 to be 1"), and the table's render helpers. 002 §2.4 and the 003 tech doc amended in the same commit. The app report preferred option 1 (the hook reads the switches); the brief's wording asked for option 2, which is what was done.)_
  - [x] **app F2 — the two skeletons live in different layers.** The chart's skeleton moved into its widget in 004; the table's is still in `pages/dashboard`. Move it to `widgets/clients-table` so the two read the same way. **[Agent: react-frontend]** _(Done 2026-09-23: `git mv` to `widgets/clients-table/ui/clients-table-skeleton.*`, exported as `ClientsTableSkeleton` beside `ClientsChartSkeleton`. The undefined `.header` class is dropped; the indent and chevron size now read `--table-indent-step`/`--table-chevron-size`, which held the same 28/16 px. Loading card measured 280 px, loaded 280 px. 001 tech doc notes the move. No test changed — page tests and e2e find placeholders by the `Skeleton` component's class, not the file's.)_
  - [x] **app F4 — the page renders and runs a timer state machine inline.** Lift it into a named hook that can be read and tested on its own, per the roadmap item. Spec 001's announcement timing (the 1 s delay that VoiceOver proved necessary) must not change; its tests are the proof. **[Agent: react-frontend]** _(Done 2026-09-23: `useDelayedFlag(active, ms)` in `pages/dashboard/model/` (not `shared/lib` — one caller), with four tests of its own, RED-proven by removing the reset in its cleanup. Also lifted the three-state derivation into a pure `toView()` beside it, the report's optional half. `LOADING_ANNOUNCE_DELAY_MS` stays 1000 in the page; the page's announcement tests pass unedited, and in the browser the status was empty at 0 s and read "Loading clients…" at 1.3 s under `?delay=2500`.)_
  - [x] **shared F2 — the boundary leaks in CSS token names and comments.** `shared/ui/tree-grid` takes its column widths from app-global `--table-*` tokens, and the word "months" appears 20 times in the "generic" layer. Rename to grid-scoped custom properties with fallbacks (`--tree-grid-col-min-w`, `--tree-grid-name-col-w`, …), have the widget or `tokens.css` set them from the Figma values, and say "columns"/"figures" in the comments. `clients-table.module.css:63` reads `--table-name-col-w` and must follow the rename. The README claims this boundary, so closing it matters. **[Agent: react-frontend]** _(Done 2026-09-23: nine grid-scoped parameters with defaults (`--tree-grid-name-col-w`, `-col-min-w`, `-row-h`, `-header-h`, `-cell-pad-start`/`-end`, `-indent-step`, `-toggle-size`, `-scroll-margin-block`), listed at the top of `tree-grid.module.css`. The widget sets them from the `--table-*` tokens on the scroller through `TreeGrid`'s existing `className`, and **D-17a's narrow-column media query moved with them into the widget** — it is a design decision, not the grid's. `clients-table.module.css`'s reveal follows the rename. Comments in the shared layer say figures/columns/rows. 002 §2.5 amended. **Left alone:** test titles in `shared/ui/tree-grid` that quote the ACs' own words ("walks the months…") — renaming them would edit tests the task didn't name. Measured in the browser: 264/160 px name column, 56 px rows, 1344/1240 px minimum width, the same as before.)_
  - [x] **shared F5 + F8 + table F5 — the grid's wiring.** Have `gridProps` carry `{ id, columnCount, onKeyDown, onFocus }` so the widget stops passing the same id and column count twice to two places that each rebuild the ids independently — today a mismatch throws nothing, focus just stops moving. Rename `TreeGrid.ColumnHeader`'s boolean `name` prop, which reads like a prop missing its value. **[Agent: react-frontend]** _(Done 2026-09-23: `gridProps` is `{ id, columnCount, onKeyDown, onFocus }` and the widget spreads it, so the id and column count are written once. `TreeGrid`'s own props are unchanged, so the tree-grid tests' `Grid` helpers still compile untouched. `name` → `nameColumn`; three test lines followed the rename, as the finding said they would. 002 §2.1 amended.)_
  - [x] **table F3 + F4 — the tests.** They find elements by CSS-module class-name substrings, which breaks on any rename, and the 500-line file repeats the same three-line setup in every test. Replace the class-name lookups with role- or text-based queries, and lift the repeated setup. These are the widget's own tests; behaviour assertions must not change. **[Agent: react-frontend]** _(Done 2026-09-23: no class-name substrings left. The label is found by its `title` (`getByTitle`/`[title]`), the avatar as the name cell's `aria-hidden` decoration that carries letters, and the hidden `Name` heading by its accessible name (the class assertion next to it was redundant). RED: an avatar on every row fails FR5-AC1 and FR1-AC1; dropping `title` fails five tests. The setup is one line (`const { user } = renderTable(…)`), synchronous now that the widget takes data, so the `findByRole('treegrid')` waits went and six tests lost `async` to satisfy `require-await`. 500 → 455 lines, 22 tests.)_
  - [x] Verify — browser: the dashboard behaves exactly as before at 1440 and 375, including the loading, failed and retry states and the `?delay=` / `?fail=1` switches. Delete the check's artifacts. **[Agent: react-frontend]** _(Done 2026-09-23, on the worktree's own `vite --port 5174`, not the owner's :5173. 1440: no sideways scroll, name column 264, rows 56, indents 16/44, one tab stop; Down, Enter, Down, Right lands on Anna's first figure by its header ids, and closing Branch 1 puts focus back on Branch 1. 375: name column 160, every figure lands exactly at the sticky edge walking left, the reveal is capped at 183 px of visible room. `?delay=2500`: silent, announced at 1 s, placeholders the same 280 px as the table. `?fail=1`: panel with the 500 detail at both widths, Retry focuses the heading and shows placeholders, every request carried `?fail=1`. Artifacts deleted.)_
  - [x] Merge the lane, run `pnpm check`, push, record ledger notes, `swarm.sh clean 005`. **[Lead]** _(Done 2026-09-23: merged --no-ff; `pnpm check` green on the merged tree — **283 unit**, 198 e2e. **Recorded for the harness:** the lane found that calling the `verify-ui` skill launches a background fork, which breaks CLAUDE.md's no-subagents rule; it stopped it and drove Playwright itself.)_

- [x] **Slice 3: The entity and the service**

  > Small and self-contained; it runs alongside nothing else because slice 2 touches the page that consumes these.
  - [x] **app F1 + F6 + F7 — the entity.** `formatBranchCount` survives only because of its own test: delete both. Trim `entities/clients`'s public API to what its consumers import. The entity imports a type from `shared/ui`; record whether that is acceptable under FSD (it is a lower layer, so it is legal) or whether the type belongs elsewhere — a note is enough if the answer is "legal". **[Agent: react-frontend]** _(Done 2026-09-23: `formatBranchCount`, `summaries.ts` and its test (4 cases) are deleted — grep found no caller anywhere in `src/` or `e2e/`. The 003 tech doc line saying it "stays" is amended in the same commit (ee1b92e), and so are the two 001 tech doc rows that described `summaries.ts` and the index. The harmless `BRANCHES` negative assertion in `dashboard-page.test.tsx` is left as it was, so no test was edited. `entities/clients/index.ts` now exports only what other slices import: seven names are gone — `CLIENTS_PATH`, `buildClientsUrl`, `fetchClients`, `FetchClientsOptions`, `ClientKind`, `formatBranchCount` and `DevSwitches` (**the last is also unused outside the slice, which the report missed**). `clientsQueryOptions` stays because the chart's refetch test imports it. **F7's answer is "legal":** the import is type-only and points to a lower layer, and no component or DOM reaches the entity — recorded at `flatten-rows.ts:2`, and `architecture.md` §6's "widgets are the only place the two meet" now says "at runtime" and names this type-level exception (8b072c9). Surprise: the worktree had no `node_modules`, so the lane ran `pnpm install --frozen-lockfile` before the gate. Gate `pnpm check:web` green: 29 files / 281 tests, Playwright 198 passed.)_
  - [x] **app F5 — `ApiConfig.isProduction` is never read.** Delete it and whatever exists only to populate it. **[Agent: nest-backend]** _(Done 2026-09-23: removed from `ApiConfig` and from `loadConfig`'s return value (`apps/api/src/config.ts`, commit feb2c7d). The **local** `isProduction` variable stays, because `devSwitches.enabled` and `corsOrigin` are still computed from it. `pnpm check:api` green: contracts 18/18, api 81/81, build plus the `NODE_ENV=production` smoke test. Surprises: (1) **two** fixtures set the field, not one as the report said — the second is `test/acceptance/clients-service.e2e-spec.ts:70` (`productionConfig`), which the report's grep missed; both lines were deleted because the typecheck fails on an unknown property, and no assertion changed — note that file is under testing-expert's `test/acceptance/`. (2) The lane reported `BLOCKED: needs context/spec/001-.../technical-considerations.md` because `context/` is outside its lane; the lead made that amendment.)_
  - [x] Merge, run `pnpm check`, push, record ledger notes, `swarm.sh clean 005`. **[Lead]** _(Done 2026-09-23: both lanes merged --no-ff, no conflicts. The lead made the amendment the backend lane was blocked on — `isProduction` dropped from the `loadConfig` row at 001 §2 line 83 — and two further stale rows the frontend lane's grep did not reach: D-12's `formatBranchCount` assumption and the web-unit test row that listed its three cases. **The gate was not run locally: the owner chose to let CI be the gate** ("no need for local gate, let's see on CI side"), so `pnpm check` for this slice is the run on the pull request, not a local one. Both lanes had already passed their own gates in their worktrees.)_

- [ ] **Slice 4: A second opinion on the component APIs**

  > The roadmap item's own last bullet: the cross-vendor reviewer reads the **public surface** of every slice — the props, the exports, the boundaries — rather than the diff.
  - [ ] Run `bin/harness/second-opinion.sh code 005 main`, triage every finding in writing, and put whatever is not worth doing now into the README's "what we would do next". **[Lead]** _(**Not run 2026-09-23 — deliberately, by the owner's call** ("let's cut the edge… I think, we are good enough to send reply to test task with our repo"). The three implementing slices closed 28 of the 29 findings; this slice was a *second* review of a surface that had just been reshaped by them, and its cost is another full cross-vendor pass plus whatever it turns up. It is not deferred into a ticket and it is not ticked: it is stated as not done, here and in the README's "what we would do next", so nobody reads a green roadmap item as a review that happened.)_
