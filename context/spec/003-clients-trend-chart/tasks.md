# Tasks — 003 Clients Trend Chart

Spec: `functional-spec.md` (FR1–FR9, 45 acceptance criteria) · How: `technical-considerations.md` (§1–§4) · Reviews: `reviews/TRIAGE.md` (spec review F1–F5, tech review F1–F3) · The consult that measured it all: `consults/react-frontend-chart-sections-20260922-234604.md`.

Everything lives in `apps/web`, so the slices are sequential and each is one lane. Slice 1 makes the chart appear; slice 2 carries the riskiest unknown — the accessibility structure, which is the reason we are not using the charting library's own keyboard layer at all.

## Standing rules

- Read `CLAUDE.md`, then `functional-spec.md`, then `technical-considerations.md`, then the consult. The tech doc is the contract, and its numbers were **measured in a real browser against a Recharts 3.10.1 prototype** — not guessed. Do not "improve" a value without re-measuring and saying so in your ledger note.
- `type` aliases over `interface`, everywhere. CSS Modules with tokens — no Tailwind, no inline style objects for static styles. FSD import direction downward only; every slice exposes `index.ts`.
- **`recharts` is imported in exactly one file, `ui/bar-plot.tsx`.** If another file seems to need it, the code is in the wrong place — report it rather than spreading the dependency.
- **`accessibilityLayer={false}`.** The library's keyboard layer fails six of this spec's criteria and has no reset API (consult Q2). Everything a screen reader or a keyboard user touches is ours.
- Tests are written with the change and proven RED first (temporarily revert, watch it fail, restore). Never delete or skip a test to make a gate green. A completion claim cites fresh output from this run.
- Pure functions are tested without Recharts and without a DOM. `yScale`, `toMonthlySeries`, `sparseMonthIndices`, `describeMonth` and the reader reducer all carry their own tests.
- **jsdom has no `ResizeObserver`**, so `ResponsiveContainer` renders no SVG there and a component test would silently assert against an empty `div` (consult Q4). Settle this in slice 1 — `initialDimension` through a prop, or a stub in the Vitest setup — and say which you chose.
- Gate before you finish: `pnpm check:web`. Not green → `PARTIAL`, with the failure quoted. The e2e server is on **5273** (`reuseExistingServer: false`); if you start a dev server for your own exploration, use another port or the gate will test the wrong tree.
- Every finished sub-task ends with `_(Done <date>: what actually happened, incl. surprises)_`.

---

- [x] **Slice 1: The chart draws — twelve stacked bars, the right axis, the right colours**

  > The drawing and nothing else: no panel, no keyboard, no narrow mode. It ends with the chart replacing the placeholder line in the upper card, and with the one number a reviewer will check — every bar agreeing with the table's Company row (FR1, FR2, FR3, FR9).
  - [x] Add `recharts@3.10.1` to `apps/web` and the four new tokens to `shared/styles/tokens.css`: `--color-channel-existing: #b29df8`, `--color-channel-organic: #f4beb4`, `--color-channel-paid: #a75e6e` (decoded from the mockup — legend swatch and bar body agree) and `--chart-plot-h: 338px`. Record the production bundle delta you measure against the previous build; the tech doc's figure is +103 kB gzip and the README will quote it. **[Agent: react-frontend]** _(Done 2026-09-23: recharts@3.10.1 pinned exactly; four tokens added. Measured bundle 361.09 kB / 110.54 kB gzip before → 674.03 kB / 202.84 kB gzip after, so **+92.3 kB gzip**, not the tech doc's +103 kB — that figure compared React alone against React+chart, not the real app. The README quotes the measured number. Vite now warns the single chunk exceeds 500 kB. The root pnpm-lock.yaml changed with it.)_
  - [x] `entities/clients/model/monthly-series.ts` per §2.1: `toMonthlySeries(data)` → twelve points, each carrying the month and one figure per channel plus the total. **Group by channel `name`, not `id`** — ids are unique per adviser, so the payload holds 30 distinct channel ids. Series come back in first-seen order, which is FR1's bottom-up order. Pure: no Recharts, no DOM. Tests RED first: the twelve totals equal the Company row's stored values, February is 221/15/14 = 250, August and January are 350, an adviser with a missing channel list is skipped rather than throwing. **[Agent: react-frontend]** _(Done 2026-09-23: groups by channel name, first-seen order, an adviser with no channel list contributes nothing. The tests read the shipped `apps/api/.../clients.json` through `?raw` rather than copying it (new `src/test/fixtures/shipped-clients.ts`, the same never-copy rule as the e2e support). RED 6/6 against a stub. All twelve totals equal the Company row; Feb 221/15/14 = 250; Aug and Jan 350. The first walk skipped every adviser and the tests caught it.)_
  - [x] `widgets/clients-chart/lib/y-scale.ts` per §2.3: `yScale(series)` → `{ ticks, top }`, step 100, `top` the first step **strictly above** the maximum. Tests RED first: 350 → top 400 with ticks 0/100/200/300/400; **400 → top 500** (strictly above, so no bar touches the ceiling — tech review F5); 0 → a sane minimum; the ticks are always evenly spaced. **[Agent: react-frontend]** _(Done 2026-09-23: step 100, `top = (floor(max/100)+1)*100`; an empty or all-zero series gets top 100 with ticks [0,100]. RED 5/5 against a stub, and an extra RED with a `ceil()` version proved both the F5 test (400 → 500) and the strictly-above test catch a ceiling-touching top.)_
  - [x] `widgets/clients-chart/ui/bar-plot.tsx` — the only file importing `recharts`. `BarChart` with three stacked `Bar`s (`stackId`), `CartesianGrid` with **horizontal dotted lines only and no vertical lines** (measured from the mockup: a column between two bars carries only the gridline crossings), `XAxis` with `formatMonth` labels, `YAxis` with `ticks={ticks} domain={[0, top]} interval={0}` — `interval={0}` is required or the library silently drops ticks for collision (consult Q1). `accessibilityLayer={false}`. No `<Legend/>`. `margin={{ right: 16 }}`. **[Agent: react-frontend]** _(Done 2026-09-23: the only file importing recharts. Left margin 0; the dotted grid is "2 4" — the lane's choice, not measured. Colours are `fill="var(--color-channel-*)"`, verified computing to rgb(178,157,248) etc. in Chromium. Axis text and grid colour come from a CSS module overriding the library's grey attributes. Added `model/channels.ts` (name → token) which throws on an unknown channel name, with a test.)_
  - [x] `widgets/clients-chart/ui/chart-legend.tsx` — a static HTML `<ul>`, centred beneath the plot, three entries with token swatches (FR3). Not the library's `<Legend/>`: it wraps at 375 and steals height from the plot (consult Q3), and as HTML it cannot be clicked into hiding a series, which is what FR3 asks for. **[Agent: react-frontend]** _(Done 2026-09-23: static `<ul>`, 8 px swatches from token classes. **Deviation, measured:** the legend text is 12 px muted, not the 14 px body `tokens.md` assigns to the legend — the lane measured the mockup. Found in the browser: the card's asymmetric 16/24 padding left the legend 4 px off the card's centre; a `margin-inline-end` of the difference centres it as the mockup does (offset now 0).)_
  - [x] `widgets/clients-chart/ui/clients-chart.tsx` and its stylesheet, per §2.6: a flex column with `min-height: var(--card-chart-min-h)`; the plot box takes **its own fixed `--chart-plot-h`** and the legend sits beneath at its natural height, so the plot's height can never be eaten by a wrapping legend (tech review F3); the card may grow. `<ResponsiveContainer width="100%" height="100%">` inside the plot box — with no definite height it renders **no SVG at all, silently, with no error** (consult Q4), so prove the SVG exists in your check. **[Agent: react-frontend]** _(Done 2026-09-23: flex column with `min-height: var(--card-chart-min-h)`, an aria-hidden plot box of fixed `--chart-plot-h` holding ResponsiveContainer 100%/100%, legend at the foot with `margin-top: auto`; `toMonthlySeries` memoised. SVG measured 1368×338 in a 430 card. **Raised for the lead:** at 338 the gridlines span 303 px against the mockup's ~320, leaving a ~40 px gap above the legend. The lane kept the specified value rather than changing it alone — see the slice 4 task added for it.)_
  - [x] Move `chart-card-skeleton.tsx` and its stylesheet into the widget as `clients-chart-skeleton.tsx`, shape unchanged, and give its plot area `--chart-plot-h` so loading and loaded measure the same. In `pages/dashboard/ui/dashboard-page.tsx` render `<ClientsChart />` in place of `<p>{formatPeriod(data.months)}</p>` and the moved skeleton in place of the old one. Delete `formatPeriod` and its tests — the chart replaces it (FR9-AC3); `formatBranchCount` stays. **[Agent: react-frontend]** _(Done 2026-09-23: `git mv` with the blocks unchanged; the skeleton's axis, bars and labels now sit in a plotArea of `--chart-plot-h` in the same frame as the loaded chart, and `loading.spec`'s sameBoxes check passes at 1440 and 375. `formatPeriod`, its tests and `.summary` deleted; `formatBranchCount` stays. Knock-on: the page tests moved to the shipped data because the old fixture's "Referral" channel makes the widget throw by design, the e2e double's channel became "Existing clients", and the period-line assertions became `expectChartLoaded()` — FR9-AC3 proven RED by putting the line back.)_
  - [x] Settle the jsdom question from the standing rules and write the first component test for the widget, so later slices have somewhere to put theirs. **[Agent: react-frontend]** _(Done 2026-09-23: chose `initialDimension` through a prop over a global ResizeObserver stub — a stub that never reports a size still draws nothing, and one that fakes a size would lie to every other test. Eight tests: SVG size, 3×12 coloured segments, segments stacked bottom-up summing to the Company row, month and 0–400 labels, five horizontal dashed lines and no vertical, drawing aria-hidden, legend order, jest-axe. **Surprise:** with motion allowed the bars never draw in jsdom (0 paths after 1.5 s), so these tests ask for reduced motion and growth is left to slice 5's e2e. RED by breaking bar-plot four ways.)_
  - [x] Verify — browser at 1440: twelve bars each in three parts, month labels "Feb 2024"…"Jan 2025", y-axis **0/100/200/300/400** (not the library's default 0/90/180/270/360), horizontal dotted gridlines with nothing vertical between months, the legend centred beneath, and for all twelve months the three segment values summing to the figure the table shows on its Company row. Confirm the SVG has non-zero width and height. Delete any screenshots or scripts the check produced. **[Agent: react-frontend]** _(Done 2026-09-23: on this worktree's own Vite at 5391, not the main checkout's 5173. Labels Feb 2024…Jan 2025; y-axis **0/100/200/300/400**, not the library's 0/90/180/270/360; five dotted horizontals spanning x 32–1352 and none vertical; legend centred; SVG 1368×338; page scrollWidth 1440. All twelve months' segments sum exactly to the treegrid's Company row, and the tallest bar's top (y 150.6) sits below the top gridline (y 112.75). Screenshot, logs and dev server removed.)_
  - [x] Merge the lane, run `pnpm check`, push, confirm CI green, record ledger notes, `swarm.sh clean 003`. **[Lead]** _(Done 2026-09-23: merged --no-ff as 2d0ecce. The gate failed first on `Cannot find module 'recharts'` — the same trap as @formkit in spec 002: the lane's dependency reaches the lead as a lockfile change, so the lead's tree needs `pnpm install --frozen-lockfile` before the gate means anything. After installing: `pnpm check` green — prettier, contracts 18, api 69 + production smoke, web lint/typecheck clean, 177 unit, 101 e2e.)_

- [x] **Slice 2: The chart answers the keyboard and the screen reader**

  > The riskiest slice, and the reason the library's own layer is off. Nothing here is Recharts': the focus target, the roving month, the announcement and the hidden table are all ours (FR5, FR6).
  - [x] `widgets/clients-chart/model/month-reader.ts` — the reducer, `{ index, open }` over `focus / blur / key / hover / leave / tap / outside / escape`. Every FR4 and FR5 rule lives here and is tested with no DOM: February on arrival; Right/Left move one month; **neither end wraps** (Left on February stays, Right on January stays); Escape closes the panel and keeps the outline; blur closes **and resets to February**, because the dashboard remembers nothing between visits (FR5-AC7). **[Agent: react-frontend]** _(Done 2026-09-23: pure `readMonth` over `{ index, open }` across all eight actions — February on focus, Left/Right clamped at both ends, Escape keeps the month, blur closes and resets. 13 tests with no DOM, 9 RED against a stub. **Surprise worth keeping:** an action that changes nothing returns the *same object*, so React skips the render and the live region never re-announces the month (FR6-AC3). Four tests for that failed against a version that always returned a new object.)_
  - [x] The frame in `clients-chart.tsx` per §2.4: a focusable `div` with `role="group"`, `aria-roledescription="chart"`, an `aria-label` naming what the chart shows and its period, and `aria-describedby` pointing at a hint that says Left and Right read each month. The whole Recharts output goes inside an `aria-hidden` wrapper carrying **`onMouseDown={e => e.preventDefault()}`** — this is load-bearing, not a nicety: the library renders twelve `<g tabindex="-1">` layers and a click otherwise moves focus *into the hidden subtree* (consult Q5). **[Agent: react-frontend]** _(Done 2026-09-23: focusable `role="group"` with `aria-roledescription="chart"`, the label naming the chart and its period, and the hint in a hidden span. The drawing sits in the aria-hidden wrapper with the `onMouseDown` preventDefault — verified in the browser: after clicking a bar, focus was **not** in the hidden subtree. Surprise: `jsx-a11y` rejects `tabIndex` and key handlers on `role="group"`; both rules disabled for this one element with the reason in the comment.)_
  - [x] A polite live region (`VisuallyHidden as="p" role="status"`) fed by `lib/describe-month.ts` → "Feb 2024: existing clients 221, new organic 15, new paid 14, total 250" (FR6-AC1). Empty string when the panel is closed. `describeMonth` is a pure function with its own test of the exact sentence. **[Agent: react-frontend]** _(Done 2026-09-23: `describeMonth(point, channels)` → "Feb 2024: existing clients 221, new organic 15, new paid 14, total 250", tested against the exact sentence and against a zero channel still being said. Polite `VisuallyHidden as="p" role="status"`, empty while closed. Surprise: a second `role="status"` made `ui.status` ambiguous in the e2e support and the page tests — both now point at the page's own region, the direct child of `<main>`.)_
  - [x] `ui/chart-data-table.tsx` — the visually-hidden table (FR6-AC2): a `<caption>`, a header row of `th scope="col"` (Month, Existing clients, New organic, New paid, Total), then twelve rows each with `th scope="row"` for the month and four `td`s. It sits **outside** the `aria-hidden` wrapper and outside the focus target, and is never itself hidden — it is the second deliberate route to the figures, not a duplicate to suppress. **[Agent: react-frontend]** _(Done 2026-09-23: caption, five `th scope="col"`, twelve rows each with `th scope="row"` and four `td`s, outside the aria-hidden wrapper and outside the focus stop; the test checks every row adds up to the Company row. Surprise: "Feb 2024" and "Existing clients" are now readable text, so slice 1's FR6-AC4 test was narrowed to allow the month only inside the table (RED with a stray span), and the page tests find the chart by its name rather than a legend word.)_
  - [x] Paint the focus ring **inset**. The focus target fills the card and `Card` has `overflow: clip`, so the project's `outline-offset: 2px` is entirely clipped and invisible even while `:focus-visible` matches (consult Q2, measured). Use `outline-offset: -2px` or an inset `box-shadow` built from `--focus-ring-width`/`--focus-ring-color`, as the tree grid already does. Prove it is visible in a screenshot before deleting it. **[Agent: react-frontend]** _(Done 2026-09-23: **differs from the brief, and the brief was wrong.** The consult's prototype made the whole card the focus target, hence its clipped-ring warning; here the focus stop is the plot box, 16/24/24 px inside the card's padding, so a 2 px-offset ring is fully visible — and an *inset* ring ran through the axis's "400" label, 2.7 px above the plot box. Used `outline-offset: calc(2 * var(--focus-ring-width))` (4 px), screenshotted whole and clear at 1440 and 375, with 10 px to spare on the left. Tech doc §2.4 corrected to match.)_
  - [x] Verify — browser at 1440: Tab from the heading lands on the chart as **one** stop with February read; Right/Left walk and clamp at both ends; Escape clears the panel and keeps focus; Tab out clears it; Shift+Tab back starts at February again; **Tab from the chart lands on the treegrid's active row** (tech doc R-3, the one thing the prototype could not test); the accessibility tree shows the group with a name of its own and **no stray axis or month text**; `@axe-core/playwright` on the card reports zero violations. Delete the check's artifacts. **[Agent: react-frontend]** _(Done 2026-09-23: Tab from the heading lands on the chart with February read; Right → Mar (267); Left ×2 stays on Feb; Right ×15 stays on Jan 2025 (350); Escape empties the live region and keeps focus, and Left then reads Dec. **Tab out lands on the treegrid's Company row** (tabindex 0) and clears the chart, and Shift+Tab back reads February again — tech doc R-3 closed, and it is now a standing assertion (`tabIntoTable`: heading → chart → Company row) rather than a one-off. ariaSnapshot shows the group with its own name, the status, the table and the legend list, with no axis or month text loose. axe on the card: 0 violations, 21 passes. Throwaway spec and screenshots deleted.)_
  - [x] Merge the lane, run `pnpm check`, push, confirm CI green, record ledger notes, `swarm.sh clean 003`. **[Lead]** _(Done 2026-09-23: merged --no-ff; `pnpm check` green on the merged tree — lint and typecheck clean, 199 unit, 101 e2e. `pnpm install --frozen-lockfile` run first out of habit now, per the slice 1 lesson.)_
  - [x] Verify — device: VoiceOver on Safari. Tab to the chart, press Right twice, and confirm each month is spoken **once**, with its total, and that the axis numbers are not read as a loose list. Chromium's accessibility tree proves the structure, not the speech — this is the same gap that amended spec 001 FR3. **[User]** _(Done 2026-09-23: owner ran it in Safari — "it works". Each month is spoken once with its total and the axis numbers are not read as a loose list, so FR6-AC1/AC3/AC4 hold on real speech, not just in the accessibility tree. One observation, and it is the spec rather than a defect: "black highlight frame does not traverse between months, highlighting only the whole chart" — FR5 keeps the outline on the chart as a whole on purpose, because a ring moving across twelve bars would mean twelve tab stops. What marks the month is the tint and the panel, which slice 3 adds; the owner saw the chart before that existed. Re-check after slice 3 rather than skipping.)_

- [x] **Slice 3: Reading a month with the pointer, and on a phone**

  > The panel, the tint and every way of dismissing them (FR4). Both tech-review majors land here.
  - [x] `ui/month-panel.tsx` — the panel's content rendered **from the widget's own state, never from the library's payload**: the month, the three channels in stacking order, then the total (FR4-AC1). Wire the controlled tooltip per §2.7 — `active`/`defaultIndex` from our state, `onMouseMove`/`onClick` → show, `onMouseLeave` → hide, `onBlur` → hide. **[Agent: react-frontend]** _(Done 2026-09-23: the panel is our own HTML inside the aria-hidden wrapper, built from `{ index, open }` — month, three parts bottom-up, then the total. It opens right of its column for Feb–Jul and left for Aug–Jan, staying inside the card at 1440 and 375 for all twelve months. **Departure from §2.7, accepted by the lead:** Recharts' `<Tooltip>` is not used at all — its `TooltipBoundingBox` keeps a "dismissed" flag tied to a coordinate, so Escape then Right at January would have stayed hidden, and it hides whenever its payload is empty. With the tint and the text already ours, it carried nothing. The month under the pointer now comes from a pure `lib/plot-geometry.ts` (`monthAt`, tested with no DOM) which also lays out the drawing. The live region speaks only while the chart has focus, so a pointer sweeping the year is not announced. RED: 3 tests failed before the change, 2 more when the guarded code was broken.)_
  - [x] **The tint is ours** (tech review F1): `cursor={false}` on the tooltip, and a tint element positioned from our `index` inside the plot box — `left = yAxisWidth + index × pitch`, `width = pitch`, filled with `--color-row-hover`, inside the `aria-hidden` wrapper. The library's own cursor follows *its* active index, which was measured disagreeing with ours. **RED first, and this test is the point of the task:** hover June, then Tab to the chart, and assert the tint, the panel and the live region all say **February**. **[Agent: react-frontend]** _(Done 2026-09-23: a separate element at `left = 32 + index × pitch`, `width = pitch`, `--color-row-hover`, inside the aria-hidden wrapper; Recharts draws no cursor at all. The key test — hover June, then Tab, and tint, panel and live region all say February — failed first because there was no tint, then failed again because arriving kept the hovered month instead of resetting. The "no library cursor" assertion was proven by temporarily forcing an active `<Tooltip>`. In Chromium the tint's centre matched each bar's centre within ±0.6 px across all twelve months.)_
  - [x] Dismissal by pointer and by tap (tech review F2): a tap inside the plot selects that month — anywhere in its column, not only on the bar — and a tap anywhere else dismisses. A document `pointerdown` listener attached **only while open** closes when the target is outside the **plot box**; scoping it to the widget root would leave the panel open on a tap on our own legend or padding. **[Agent: react-frontend]** _(Done 2026-09-23: a pointerdown on the plot box selects the month of the column tapped, including above the bar; a tap on the axes dismisses; a document pointerdown listener attached only while open dismisses anything outside the plot box. A touch pointer's leave is ignored, or the panel would close as the finger lifts. Each rule proven RED by breaking it: scoped to the widget root, the legend and padding tests failed; with no listener, the table-card test failed.)_
  - [x] Verify — browser at 1440 and under touch emulation at 375: hover a month → panel and tint on that month only; pointer off → both gone; 20 fast enter/exit cycles leave nothing behind; tap a month → panel; tap another → replaced; tap the legend, the card's padding, and the table card → dismissed each time; tap a column above its bar → that month is selected. Delete the check's artifacts. **[Agent: react-frontend]** _(Done 2026-09-23: Playwright on a private port (5383) with the shipped data mocked — **26/26 checks**: hover on each month, pointer off, 20 fast enter/exit cycles, hover-then-Tab showing February, Escape then Right at January; at 375, tapping each month, a tap replacing the open one, a tap on a column above its bar selecting it, and taps on the legend, the card's padding, the table card and the month labels each dismissing. No horizontal scroll, no console errors. **Surprise, and a real bug:** the first run left a panel behind after 20 fast cycles — each hover re-rendered BarPlot, replacing the node under the pointer, so Chromium sent no `pointerout` and React's `onPointerLeave` never fired. Fixed by memoising BarPlot and listening for the native `pointerleave`, with a unit test that failed before the fix. Artifacts deleted, dev server stopped.)_
  - [x] Merge the lane, run `pnpm check`, push, confirm CI green, record ledger notes, `swarm.sh clean 003`. **[Lead]** _(Done 2026-09-23: merged --no-ff; `pnpm check` green on the merged tree — lint and typecheck clean, 221 unit, 101 e2e.)_

- [ ] **Slice 4: The whole year on a phone**

  > All twelve bars at 375 with labels that fit, and a plot whose height never changes (FR8).
  - [ ] `widgets/clients-chart/lib/month-ticks.ts`: `sparseMonthIndices(n)` → `[0, 3, 6, 9, n-1]`, and the label for each mode — full "Feb 2024" everywhere when there is room, and "Feb 2024", "May", "Aug", "Nov", "Jan 2025" when there is not (FR8, amended 2026-09-23). Pure, tested. **No `interval` value can produce that index set** — it takes a number or a preserve mode, never a list — so the plot passes explicit `ticks` with `interval={0}` (consult Q3). **[Agent: react-frontend]**
  - [ ] Switch modes on the **chart's own measured width** via `ResponsiveContainer`'s `onResize`, not a viewport media query and **not the table's ~600 px breakpoint** — twelve one-line labels still overlap by 14.5 px at 600 and only come clear at about 780 px (consult Q3, measured). Switch to the sparse mode when a band is narrower than about 60 px. Keep the `XAxis` height **constant across both modes**, or the plot's height changes with the label mode. **[Agent: react-frontend]**
  - [ ] **Lead's call from slice 1:** at `--chart-plot-h: 338px` the gridlines span 303 px, against the design's plot area of ~320 (`context/inbox/design/tokens.md`), leaving a ~40 px gap above the legend. Raise the token so the **gridlines** measure ~320 — the box must hold the plot plus the x-axis labels, so derive it rather than guessing — and re-check that loading and loaded still measure the same, that the card still fits its 430 px, and that the plot height is identical at 375 and 1440. **[Agent: react-frontend]**
  - [ ] Verify — browser at 375, 440, 600, 780 and 1440: at 375 all twelve bars are inside the card, the five labels read "Feb 2024", "May", "Aug", "Nov", "Jan 2025" with **no overlap** (measure their painted boxes, do not eyeball), the page has no horizontal scrollbar, and the plot box measures **the same height as at 1440 with the real legend rendered** (tech review F3). Note the bar width you measure — the tech doc expects 17–18 px. Delete the check's artifacts. **[Agent: react-frontend]**
  - [ ] Merge the lane, run `pnpm check`, push, confirm CI green, record ledger notes, `swarm.sh clean 003`. **[Lead]**
  - [ ] Verify — device: open the dashboard on a real phone. The five labels are readable and do not collide, tapping a month opens its panel and tapping the legend dismisses it, and nothing scrolls sideways. **[User]**

- [ ] **Slice 5: Movement, and the honest states**

  > The last of the behaviour (FR7, FR9) — mostly confirming that what spec 001 built still holds once a chart is in the card.
  - [ ] Confirm reduced motion needs **no code**: `Bar`'s default `isAnimationActive: 'auto'` resolves to `!prefersReducedMotion` and the library subscribes to the media query, so leave the prop unset (consult Q5, measured: six distinct bar heights during load normally, exactly one under `reduce`). Memoise `toMonthlySeries` so a refetch with unchanged figures cannot replay the growth (FR7-AC1). If your measurement disagrees with the consult's, say so and set the prop. **[Agent: react-frontend]**
  - [ ] Verify — browser: bars grow once when the figures land and are fully drawn from the first frame under `prefers-reduced-motion: reduce`; with `?delay=2500` the card shows the placeholder blocks and then the chart, with no jump in the card's height; with `?fail=1` the error panel and its Retry appear in place of the chart, and Retry against a recovered service brings the chart back; the line "12 months · Feb 2024 – Jan 2025" appears nowhere. Delete the check's artifacts. **[Agent: react-frontend]**
  - [ ] Merge the lane, run `pnpm check`, push, confirm CI green, record ledger notes, `swarm.sh clean 003`. **[Lead]**

- [ ] **Slice 6: Feature Testing & Regression**

  > Verifies the whole feature end-to-end against functional-spec.md, run after all implementation slices are complete.
  - [ ] Read functional-spec.md acceptance criteria in full. Generate acceptance-level tests that verify the entire feature as a whole — not individual slices. Cover applicable layers (unit for pure logic, integration for service interactions, e2e for user flows) based on the project's testing stack. Write tests with RED validation (must fail before implementation is confirmed done). Annotate each test with `@spec: 003-clients-trend-chart` and `@regression` if suitable for long-term regression. **[Agent: testing-expert]**
  - [ ] **Raised by slice 3:** that lane proved F1 and F2 in a throwaway Playwright run (26/26) and then deleted it, as a Verify step must. Neither has a committed spec. Write them: hover June → Tab → February in the tint, the panel **and** the live region; and taps on the legend, the card's padding and the table card each dismissing. These are the two regressions the tech review bought and they must not live only in a lane's memory. **[Agent: testing-expert]**
  - [ ] Run all generated tests. All must pass. Fix any failures before proceeding. **[Agent: testing-expert]**
  - [ ] The agreement test a reviewer will look for: in one page, read all twelve bars from the chart and all twelve figures from the table's Company row, and assert they match month by month (FR1-AC2). It must be impossible to pass while only one card is right. **[Agent: testing-expert]**
  - [ ] Run the table and chart specs in **WebKit** as well as Chromium. Everything in the tech doc was measured in Chromium only, and spec 002 found a WebKit-only defect in exactly this territory (`position: sticky` on `<tbody>`). Report any difference rather than papering over it. **[Agent: testing-expert]**
  - [ ] Merge the lane, run `pnpm check`, push, confirm CI green on the PR, record ledger notes, `swarm.sh clean 003`. **[Lead]**
