# Specialist Agents Coverage

Generated by `/awos:hire` on 2026-09-21. Re-run `/awos:hire` to refresh — this file goes stale as soon as `.claude/agents/` or `context/product/architecture.md` changes.

## Coverage by Technology

| Technology | Recommended Subagent Role | Status | Agent |
| ---------- | ------------------------- | ------ | ----- |
| React 19 | `react-frontend` | ✅ Covered | react-frontend |
| TypeScript (strict) | `react-frontend` / `nest-backend` | ✅ Covered — `typescript-development` skill on both | react-frontend |
| Vite 8 | `react-frontend` | ✅ Covered | react-frontend |
| Feature-Sliced Design | `react-frontend` | ✅ Covered — `react-feature-sliced-design` skill | react-frontend |
| CSS Modules / design tokens | `react-frontend` | ✅ Covered | react-frontend |
| TanStack Query v5 | `react-frontend` | ✅ Covered — `react-best-practices` covers data fetching | react-frontend |
| Recharts 3 | `react-frontend` | ⚠️ Partial — no Recharts skill in the registry; general knowledge + architecture §1 | react-frontend |
| WAI-ARIA APG treegrid / accessibility | `react-frontend` | ⚠️ Partial — no accessibility skill in the registry; pattern spelled out in the agent file | react-frontend |
| NestJS | `nest-backend` | ⚠️ Partial — no NestJS skill in the registry; general knowledge + architecture §1–2 | nest-backend |
| Node 22 / REST / zod contracts | `nest-backend` | ✅ Covered | nest-backend |
| Vitest + React Testing Library | `react-frontend` | ✅ Covered | react-frontend |
| Playwright + axe-core (e2e) | `frontend-qa` | ✅ Covered — `testing-expert` with `verify-ui` skill and the `playwright` MCP | testing-expert |
| Jest + supertest (API tests) | `nest-backend` | ✅ Covered | nest-backend |
| pnpm workspaces / ESLint 9 / Prettier | `developer` (scaffold) | ✅ Covered — generic implementer for root-level tasks | developer |
| GitHub Actions CI | `developer` (scaffold) | ✅ Covered | developer |

## Registered Specialist Subagents

| Name | Description | Skills |
| ---- | ----------- | ------ |
| react-frontend | Frontend lane for the Clients dashboard — React 19 + TypeScript on Vite, Feature-Sliced Design, CSS Modules, TanStack Query, Recharts, and the hand-built WAI-ARIA treegrid table. Delegate any task under apps/web/ — pages, widgets, entities, shared/ui, styling from the Figma tokens, accessibility, and Vitest + React Testing Library tests. | react-feature-sliced-design, typescript-development, react-best-practices |
| nest-backend | Backend lane for the Clients dashboard — NestJS + TypeScript on Node 22 serving the read-only REST API, and the shared wire contract in packages/contracts. Delegate any task under apps/api/ or packages/contracts/ — modules, controllers, services, the JSON repository, the data-consistency check, zod schemas, and Jest + supertest API tests. | typescript-development |
| testing-expert | Feature-level QA agent for AWOS projects. Analyzes functional-spec.md acceptance criteria and generates comprehensive acceptance tests (unit, integration, e2e) that verify the entire feature works as described. Called at the end of feature development via the Feature Testing & Regression slice in tasks.md — not per-slice. Supports RED validation and annotates tests with @spec and @regression for regression suite management. | verify-ui |
| developer | Generic single-lane implementer for projects that have not yet hired domain agents with /awos:hire. Implements the tasks in its lane brief, runs the lane gate, reports in the LANE format. | — |
| reviewer | Fallback second-opinion reviewer used only when the cross-vendor reviewer (Codex) is unavailable. Reviews specs and diffs adversarially, reports findings in the harness F<n> format, never edits files. | — |

## Installed Hooks

| Name | Event | Command | Description |
| ---- | ----- | ------- | ----------- |
| — | PreToolUse (matcher `Bash`) | `"$CLAUDE_PROJECT_DIR"/.claude/hooks/guard.sh` | — |
| — | PostToolUse (matcher `Edit\|Write\|MultiEdit`) | `"$CLAUDE_PROJECT_DIR"/.claude/hooks/format.sh` | — |

_Both pre-date this run (harness-kit). The registry returned no hooks for "format code automatically after edits", "run lint and tests before completing work" or "block dangerous git commands"; none were installed._

## Gaps

- Recharts 3: no registry skill → `react-frontend` uses general knowledge; keep `toMonthlySeries` a pure, unit-tested function so chart-library specifics stay thin.
- Accessibility / WAI-ARIA treegrid: no registry skill → the APG keyboard model and ARIA attributes are spelled out in `.claude/agents/react-frontend.md`; `testing-expert` audits with axe. A custom `.claude/skills/a11y-treegrid/` would raise confidence.
- NestJS: no registry skill → `nest-backend` uses general knowledge; the API is one module, so the risk is low.

## Lane wiring (harness.json)

| Lane | Owns | Gate |
| ---- | ---- | ---- |
| developer | `./` (scaffold / root-level; runs alone in its slice) | `pnpm check` |
| react-frontend | `apps/web/` | `pnpm check:web` |
| nest-backend | `apps/api/`, `packages/contracts/` | `pnpm check:api` |
| testing-expert | `apps/web/e2e/`, `apps/api/test/` (test files only) | `pnpm check` |

_Tasks-time rule that follows from the ownership: `packages/contracts` is defined by `nest-backend` in an earlier slice than the frontend work that imports it; `react-frontend` reports `BLOCKED: needs packages/contracts/…` rather than editing it._
